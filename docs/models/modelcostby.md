# ModelCostBy

## Example Usage

```typescript
import { ModelCostBy } from "galileo-generated/models";

let value: ModelCostBy = "characters";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"tokens" | "characters" | Unrecognized<string>
```