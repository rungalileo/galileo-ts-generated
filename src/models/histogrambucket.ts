/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type HistogramBucket = {
  /**
   * Lower bound of the histogram bucket (inclusive)
   */
  lower: number;
  /**
   * Upper bound of the histogram bucket (exclusive, but inclusive for the last bucket)
   */
  upper: number;
  /**
   * Number of data points that fall within this bucket
   */
  count: number;
};

/** @internal */
export const HistogramBucket$inboundSchema: z.ZodMiniType<
  HistogramBucket,
  unknown
> = z.object({
  lower: types.number(),
  upper: types.number(),
  count: types.number(),
});

export function histogramBucketFromJSON(
  jsonString: string,
): SafeParseResult<HistogramBucket, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => HistogramBucket$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'HistogramBucket' from JSON`,
  );
}
