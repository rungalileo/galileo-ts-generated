/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type RecomputeSettingsRuns = {
  mode: "runs";
  runIds: Array<string>;
};

/** @internal */
export const RecomputeSettingsRuns$inboundSchema: z.ZodMiniType<
  RecomputeSettingsRuns,
  unknown
> = z.pipe(
  z.object({
    mode: types.literal("runs"),
    run_ids: z.array(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "run_ids": "runIds",
    });
  }),
);
/** @internal */
export type RecomputeSettingsRuns$Outbound = {
  mode: "runs";
  run_ids: Array<string>;
};

/** @internal */
export const RecomputeSettingsRuns$outboundSchema: z.ZodMiniType<
  RecomputeSettingsRuns$Outbound,
  RecomputeSettingsRuns
> = z.pipe(
  z.object({
    mode: z.literal("runs"),
    runIds: z.array(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      runIds: "run_ids",
    });
  }),
);

export function recomputeSettingsRunsToJSON(
  recomputeSettingsRuns: RecomputeSettingsRuns,
): string {
  return JSON.stringify(
    RecomputeSettingsRuns$outboundSchema.parse(recomputeSettingsRuns),
  );
}
export function recomputeSettingsRunsFromJSON(
  jsonString: string,
): SafeParseResult<RecomputeSettingsRuns, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RecomputeSettingsRuns$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RecomputeSettingsRuns' from JSON`,
  );
}
