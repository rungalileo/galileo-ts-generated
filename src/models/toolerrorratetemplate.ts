/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 81a59e8fc2ea
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  FewShotExample,
  FewShotExample$inboundSchema,
  FewShotExample$Outbound,
  FewShotExample$outboundSchema,
} from "./fewshotexample.js";

/**
 * Template for the tool error rate metric,
 *
 * @remarks
 * containing all the info necessary to send the tool error rate prompt.
 */
export type ToolErrorRateTemplate = {
  metricSystemPrompt?: string | undefined;
  metricDescription?: string | undefined;
  valueFieldName?: string | undefined;
  /**
   * Field name to look for in the chainpoll response, for the explanation.
   */
  explanationFieldName?: string | undefined;
  template?: string | undefined;
  metricFewShotExamples?: Array<FewShotExample> | undefined;
  /**
   * Response schema for the output
   */
  responseSchema?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const ToolErrorRateTemplate$inboundSchema: z.ZodMiniType<
  ToolErrorRateTemplate,
  unknown
> = z.pipe(
  z.object({
    metric_system_prompt: z._default(
      types.string(),
      "One or more functions have been called, and you will receive their output. The output format could be a string containing the tool's result, it could be in JSON or XML format with additional metadata and information, or it could be a list of the outputs in any such format.\n\nYour task is to determine whether at least one function call didn't execute correctly and errored out. If at least one call failed, then you should consider the entire call as a failure. \nYou should NOT evaluate any other aspect of the tool call. In particular you should not evaluate whether the output is well formatted, coherent or contains spelling mistakes.\n\nIf you conclude that the call failed, provide an explanation as to why. You may summarize any error message you encounter. If the call was successful, no explanation is needed.\n\nRespond in the following JSON format:\n\n```\n{\n   \"function_errored_out\": boolean,\n   \"explanation\": string\n}\n```\n\n- **\"function_errored_out\"**: Use `false` if all tool calls were successful, and `true` if at least one errored out.\n\n- **\"explanation\"**: If a tool call failed, provide your step-by-step reasoning to determine why it might have failed. If all tool calls were succesful, leave this blank.\n\nYou must respond with a valid JSON object; don't forget to escape special characters.",
    ),
    metric_description: z._default(
      types.string(),
      "I have a multi-turn chatbot application where the assistant is an agent that has access to tools. I want a metric to evaluate whether a tool invocation was successful or if it resulted in an error.",
    ),
    value_field_name: z._default(types.string(), "function_errored_out"),
    explanation_field_name: z._default(types.string(), "explanation"),
    template: z._default(types.string(), "Tools output:\n```\n{response}\n```"),
    metric_few_shot_examples: types.optional(
      z.array(FewShotExample$inboundSchema),
    ),
    response_schema: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metric_system_prompt": "metricSystemPrompt",
      "metric_description": "metricDescription",
      "value_field_name": "valueFieldName",
      "explanation_field_name": "explanationFieldName",
      "metric_few_shot_examples": "metricFewShotExamples",
      "response_schema": "responseSchema",
    });
  }),
);
/** @internal */
export type ToolErrorRateTemplate$Outbound = {
  metric_system_prompt: string;
  metric_description: string;
  value_field_name: string;
  explanation_field_name: string;
  template: string;
  metric_few_shot_examples?: Array<FewShotExample$Outbound> | undefined;
  response_schema?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const ToolErrorRateTemplate$outboundSchema: z.ZodMiniType<
  ToolErrorRateTemplate$Outbound,
  ToolErrorRateTemplate
> = z.pipe(
  z.object({
    metricSystemPrompt: z._default(
      z.string(),
      "One or more functions have been called, and you will receive their output. The output format could be a string containing the tool's result, it could be in JSON or XML format with additional metadata and information, or it could be a list of the outputs in any such format.\n\nYour task is to determine whether at least one function call didn't execute correctly and errored out. If at least one call failed, then you should consider the entire call as a failure. \nYou should NOT evaluate any other aspect of the tool call. In particular you should not evaluate whether the output is well formatted, coherent or contains spelling mistakes.\n\nIf you conclude that the call failed, provide an explanation as to why. You may summarize any error message you encounter. If the call was successful, no explanation is needed.\n\nRespond in the following JSON format:\n\n```\n{\n   \"function_errored_out\": boolean,\n   \"explanation\": string\n}\n```\n\n- **\"function_errored_out\"**: Use `false` if all tool calls were successful, and `true` if at least one errored out.\n\n- **\"explanation\"**: If a tool call failed, provide your step-by-step reasoning to determine why it might have failed. If all tool calls were succesful, leave this blank.\n\nYou must respond with a valid JSON object; don't forget to escape special characters.",
    ),
    metricDescription: z._default(
      z.string(),
      "I have a multi-turn chatbot application where the assistant is an agent that has access to tools. I want a metric to evaluate whether a tool invocation was successful or if it resulted in an error.",
    ),
    valueFieldName: z._default(z.string(), "function_errored_out"),
    explanationFieldName: z._default(z.string(), "explanation"),
    template: z._default(z.string(), "Tools output:\n```\n{response}\n```"),
    metricFewShotExamples: z.optional(z.array(FewShotExample$outboundSchema)),
    responseSchema: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      metricSystemPrompt: "metric_system_prompt",
      metricDescription: "metric_description",
      valueFieldName: "value_field_name",
      explanationFieldName: "explanation_field_name",
      metricFewShotExamples: "metric_few_shot_examples",
      responseSchema: "response_schema",
    });
  }),
);

export function toolErrorRateTemplateToJSON(
  toolErrorRateTemplate: ToolErrorRateTemplate,
): string {
  return JSON.stringify(
    ToolErrorRateTemplate$outboundSchema.parse(toolErrorRateTemplate),
  );
}
export function toolErrorRateTemplateFromJSON(
  jsonString: string,
): SafeParseResult<ToolErrorRateTemplate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ToolErrorRateTemplate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ToolErrorRateTemplate' from JSON`,
  );
}
