/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6766d86b7143
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * GCS config response model — credentials are never exposed in GET responses.
 */
export type VertexAIGCSConfigResponse = {
  bucketName: string;
  objectPathPrefix: string;
};

/** @internal */
export const VertexAIGCSConfigResponse$inboundSchema: z.ZodMiniType<
  VertexAIGCSConfigResponse,
  unknown
> = z.pipe(
  z.object({
    bucket_name: types.string(),
    object_path_prefix: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "bucket_name": "bucketName",
      "object_path_prefix": "objectPathPrefix",
    });
  }),
);

export function vertexAIGCSConfigResponseFromJSON(
  jsonString: string,
): SafeParseResult<VertexAIGCSConfigResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => VertexAIGCSConfigResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'VertexAIGCSConfigResponse' from JSON`,
  );
}
