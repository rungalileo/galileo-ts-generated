/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 696751e472ae
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type BucketedMetric = {
  name: string;
  buckets: { [k: string]: number };
  average?: number | null | undefined;
};

/** @internal */
export const BucketedMetric$inboundSchema: z.ZodMiniType<
  BucketedMetric,
  unknown
> = z.object({
  name: types.string(),
  buckets: z.record(z.string(), types.number()),
  average: z.optional(z.nullable(types.number())),
});

export function bucketedMetricFromJSON(
  jsonString: string,
): SafeParseResult<BucketedMetric, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BucketedMetric$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BucketedMetric' from JSON`,
  );
}
