/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a31e5a86240b
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  HistogramBucket,
  HistogramBucket$inboundSchema,
} from "./histogrambucket.js";
import {
  HistogramStrategy,
  HistogramStrategy$inboundSchema,
} from "./histogramstrategy.js";

export type Histogram = {
  strategy: HistogramStrategy;
  /**
   * List of bin edges (monotonically increasing, length = number of buckets + 1)
   */
  edges: Array<number>;
  /**
   * List of histogram buckets containing the binned data
   */
  buckets: Array<HistogramBucket>;
  /**
   * Total number of data points in the histogram
   */
  total: number;
};

/** @internal */
export const Histogram$inboundSchema: z.ZodMiniType<Histogram, unknown> = z
  .object({
    strategy: HistogramStrategy$inboundSchema,
    edges: z.array(types.number()),
    buckets: z.array(HistogramBucket$inboundSchema),
    total: types.number(),
  });

export function histogramFromJSON(
  jsonString: string,
): SafeParseResult<Histogram, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Histogram$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Histogram' from JSON`,
  );
}
