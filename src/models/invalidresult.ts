/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type InvalidResult = {
  resultType: "invalid";
  errorMessage: string;
};

/** @internal */
export const InvalidResult$inboundSchema: z.ZodMiniType<
  InvalidResult,
  unknown
> = z.pipe(
  z.object({
    result_type: z._default(types.literal("invalid"), "invalid"),
    error_message: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "result_type": "resultType",
      "error_message": "errorMessage",
    });
  }),
);

export function invalidResultFromJSON(
  jsonString: string,
): SafeParseResult<InvalidResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InvalidResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InvalidResult' from JSON`,
  );
}
