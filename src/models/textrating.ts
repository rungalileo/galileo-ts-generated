/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 3de72a05cea1
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type TextRating = {
  feedbackType: "text";
  value: string;
};

/** @internal */
export const TextRating$inboundSchema: z.ZodMiniType<TextRating, unknown> = z
  .pipe(
    z.object({
      feedback_type: types.literal("text"),
      value: types.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "feedback_type": "feedbackType",
      });
    }),
  );

export function textRatingFromJSON(
  jsonString: string,
): SafeParseResult<TextRating, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TextRating$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextRating' from JSON`,
  );
}
