/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4386927aa443
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type TextConstraints = {
  annotationType: "text";
};

/** @internal */
export const TextConstraints$inboundSchema: z.ZodMiniType<
  TextConstraints,
  unknown
> = z.pipe(
  z.object({
    annotation_type: types.literal("text"),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
    });
  }),
);
/** @internal */
export type TextConstraints$Outbound = {
  annotation_type: "text";
};

/** @internal */
export const TextConstraints$outboundSchema: z.ZodMiniType<
  TextConstraints$Outbound,
  TextConstraints
> = z.pipe(
  z.object({
    annotationType: z.literal("text"),
  }),
  z.transform((v) => {
    return remap$(v, {
      annotationType: "annotation_type",
    });
  }),
);

export function textConstraintsToJSON(
  textConstraints: TextConstraints,
): string {
  return JSON.stringify(TextConstraints$outboundSchema.parse(textConstraints));
}
export function textConstraintsFromJSON(
  jsonString: string,
): SafeParseResult<TextConstraints, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TextConstraints$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextConstraints' from JSON`,
  );
}
