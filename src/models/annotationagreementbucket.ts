/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 53b224dc0d61
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AnnotationAgreementBucket = {
  minInclusive: number;
  maxExclusive: number | null;
  count: number;
};

/** @internal */
export const AnnotationAgreementBucket$inboundSchema: z.ZodMiniType<
  AnnotationAgreementBucket,
  unknown
> = z.pipe(
  z.object({
    min_inclusive: types.number(),
    max_exclusive: types.nullable(types.number()),
    count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "min_inclusive": "minInclusive",
      "max_exclusive": "maxExclusive",
    });
  }),
);

export function annotationAgreementBucketFromJSON(
  jsonString: string,
): SafeParseResult<AnnotationAgreementBucket, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnotationAgreementBucket$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnotationAgreementBucket' from JSON`,
  );
}
