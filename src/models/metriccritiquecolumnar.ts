/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  MetricCritiqueContent,
  MetricCritiqueContent$inboundSchema,
} from "./metriccritiquecontent.js";

export type MetricCritiqueColumnar = {
  id: string;
  isComputed: boolean;
  revisedExplanation: string | null;
  critiqueInfo: MetricCritiqueContent;
};

/** @internal */
export const MetricCritiqueColumnar$inboundSchema: z.ZodMiniType<
  MetricCritiqueColumnar,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    is_computed: types.boolean(),
    revised_explanation: types.nullable(types.string()),
    critique_info: MetricCritiqueContent$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "is_computed": "isComputed",
      "revised_explanation": "revisedExplanation",
      "critique_info": "critiqueInfo",
    });
  }),
);

export function metricCritiqueColumnarFromJSON(
  jsonString: string,
): SafeParseResult<MetricCritiqueColumnar, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetricCritiqueColumnar$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetricCritiqueColumnar' from JSON`,
  );
}
