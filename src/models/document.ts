/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d5b45ebda84c
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type DocumentMetadata = boolean | string | number | number;

export type Document = {
  /**
   * Content of the document.
   */
  pageContent: string;
  metadata?: { [k: string]: boolean | string | number | number } | undefined;
};

/** @internal */
export const DocumentMetadata$inboundSchema: z.ZodMiniType<
  DocumentMetadata,
  unknown
> = smartUnion([
  types.boolean(),
  types.string(),
  types.number(),
  types.number(),
]);
/** @internal */
export type DocumentMetadata$Outbound = boolean | string | number | number;

/** @internal */
export const DocumentMetadata$outboundSchema: z.ZodMiniType<
  DocumentMetadata$Outbound,
  DocumentMetadata
> = smartUnion([z.boolean(), z.string(), z.int(), z.number()]);

export function documentMetadataToJSON(
  documentMetadata: DocumentMetadata,
): string {
  return JSON.stringify(
    DocumentMetadata$outboundSchema.parse(documentMetadata),
  );
}
export function documentMetadataFromJSON(
  jsonString: string,
): SafeParseResult<DocumentMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DocumentMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DocumentMetadata' from JSON`,
  );
}

/** @internal */
export const Document$inboundSchema: z.ZodMiniType<Document, unknown> = z.pipe(
  z.object({
    page_content: types.string(),
    metadata: types.optional(
      z.record(
        z.string(),
        smartUnion([
          types.boolean(),
          types.string(),
          types.number(),
          types.number(),
        ]),
      ),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "page_content": "pageContent",
    });
  }),
);
/** @internal */
export type Document$Outbound = {
  page_content: string;
  metadata?: { [k: string]: boolean | string | number | number } | undefined;
};

/** @internal */
export const Document$outboundSchema: z.ZodMiniType<
  Document$Outbound,
  Document
> = z.pipe(
  z.object({
    pageContent: z.string(),
    metadata: z.optional(
      z.record(
        z.string(),
        smartUnion([z.boolean(), z.string(), z.int(), z.number()]),
      ),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      pageContent: "page_content",
    });
  }),
);

export function documentToJSON(document: Document): string {
  return JSON.stringify(Document$outboundSchema.parse(document));
}
export function documentFromJSON(
  jsonString: string,
): SafeParseResult<Document, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Document$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Document' from JSON`,
  );
}
