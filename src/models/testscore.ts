/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 54400aafbce2
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { NodeType, NodeType$inboundSchema } from "./nodetype.js";

export type Score = number | number | string | boolean;

export type TestScore = {
  nodeType: NodeType;
  score?: number | number | string | boolean | null | undefined;
};

/** @internal */
export const Score$inboundSchema: z.ZodMiniType<Score, unknown> = smartUnion([
  types.number(),
  types.number(),
  types.string(),
  types.boolean(),
]);

export function scoreFromJSON(
  jsonString: string,
): SafeParseResult<Score, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Score$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Score' from JSON`,
  );
}

/** @internal */
export const TestScore$inboundSchema: z.ZodMiniType<TestScore, unknown> = z
  .pipe(
    z.object({
      node_type: NodeType$inboundSchema,
      score: z.optional(
        z.nullable(
          smartUnion([
            types.number(),
            types.number(),
            types.string(),
            types.boolean(),
          ]),
        ),
      ),
    }),
    z.transform((v) => {
      return remap$(v, {
        "node_type": "nodeType",
      });
    }),
  );

export function testScoreFromJSON(
  jsonString: string,
): SafeParseResult<TestScore, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TestScore$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TestScore' from JSON`,
  );
}
