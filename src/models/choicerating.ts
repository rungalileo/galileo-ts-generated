/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a4a717f87529
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ChoiceRating = {
  feedbackType: "choice";
  value: string;
};

/** @internal */
export const ChoiceRating$inboundSchema: z.ZodMiniType<ChoiceRating, unknown> =
  z.pipe(
    z.object({
      feedback_type: types.literal("choice"),
      value: types.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "feedback_type": "feedbackType",
      });
    }),
  );

export function choiceRatingFromJSON(
  jsonString: string,
): SafeParseResult<ChoiceRating, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ChoiceRating$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ChoiceRating' from JSON`,
  );
}
