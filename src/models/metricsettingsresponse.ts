/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { ScorerConfig, ScorerConfig$inboundSchema } from "./scorerconfig.js";
import { SegmentFilter, SegmentFilter$inboundSchema } from "./segmentfilter.js";

export type MetricSettingsResponse = {
  scorers: Array<ScorerConfig>;
  /**
   * List of segment filters to apply to the run.
   */
  segmentFilters?: Array<SegmentFilter> | null | undefined;
};

/** @internal */
export const MetricSettingsResponse$inboundSchema: z.ZodMiniType<
  MetricSettingsResponse,
  unknown
> = z.pipe(
  z.object({
    scorers: z.array(ScorerConfig$inboundSchema),
    segment_filters: z.optional(
      z.nullable(z.array(SegmentFilter$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "segment_filters": "segmentFilters",
    });
  }),
);

export function metricSettingsResponseFromJSON(
  jsonString: string,
): SafeParseResult<MetricSettingsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetricSettingsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetricSettingsResponse' from JSON`,
  );
}
