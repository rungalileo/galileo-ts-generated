/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { FeedbackType, FeedbackType$inboundSchema } from "./feedbacktype.js";

export type FeedbackRatingInfoValue = boolean | number | string | Array<string>;

export type FeedbackRatingInfo = {
  feedbackType: FeedbackType;
  value: boolean | number | string | Array<string>;
  explanation: string | null;
};

/** @internal */
export const FeedbackRatingInfoValue$inboundSchema: z.ZodMiniType<
  FeedbackRatingInfoValue,
  unknown
> = smartUnion([
  types.boolean(),
  types.number(),
  types.string(),
  z.array(types.string()),
]);

export function feedbackRatingInfoValueFromJSON(
  jsonString: string,
): SafeParseResult<FeedbackRatingInfoValue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FeedbackRatingInfoValue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FeedbackRatingInfoValue' from JSON`,
  );
}

/** @internal */
export const FeedbackRatingInfo$inboundSchema: z.ZodMiniType<
  FeedbackRatingInfo,
  unknown
> = z.pipe(
  z.object({
    feedback_type: FeedbackType$inboundSchema,
    value: smartUnion([
      types.boolean(),
      types.number(),
      types.string(),
      z.array(types.string()),
    ]),
    explanation: types.nullable(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feedback_type": "feedbackType",
    });
  }),
);

export function feedbackRatingInfoFromJSON(
  jsonString: string,
): SafeParseResult<FeedbackRatingInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FeedbackRatingInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FeedbackRatingInfo' from JSON`,
  );
}
