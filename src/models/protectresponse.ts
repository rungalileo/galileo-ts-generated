/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cb5798d7fa23
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ExecutionStatus,
  ExecutionStatus$inboundSchema,
} from "./executionstatus.js";
import { TraceMetadata, TraceMetadata$inboundSchema } from "./tracemetadata.js";

/**
 * Protect response schema with custom OpenAPI title.
 */
export type ProtectResponse = {
  /**
   * Status of the execution.
   */
  status?: ExecutionStatus | undefined;
  /**
   * Text from the request after processing the rules.
   */
  text: string;
  traceMetadata: TraceMetadata;
  [additionalProperties: string]: unknown;
};

/** @internal */
export const ProtectResponse$inboundSchema: z.ZodMiniType<
  ProtectResponse,
  unknown
> = z.pipe(
  z.catchall(
    z.object({
      status: types.optional(ExecutionStatus$inboundSchema),
      text: types.string(),
      trace_metadata: TraceMetadata$inboundSchema,
    }),
    z.any(),
  ),
  z.transform((v) => {
    return remap$(v, {
      "trace_metadata": "traceMetadata",
    });
  }),
);

export function protectResponseFromJSON(
  jsonString: string,
): SafeParseResult<ProtectResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ProtectResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ProtectResponse' from JSON`,
  );
}
