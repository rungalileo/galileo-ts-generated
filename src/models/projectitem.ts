/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Permission, Permission$inboundSchema } from "./permission.js";
import { ProjectLabels, ProjectLabels$inboundSchema } from "./projectlabels.js";
import { UserInfo, UserInfo$inboundSchema } from "./userinfo.js";

/**
 * Represents a single project item for the UI list.
 */
export type ProjectItem = {
  id: string;
  permissions?: Array<Permission> | undefined;
  name: string;
  createdAt: Date;
  updatedAt: Date;
  bookmark: boolean;
  /**
   * Count of runs with task_type=15
   */
  numLogstreams?: number | null | undefined;
  /**
   * Count of runs with task_type=16
   */
  numExperiments?: number | null | undefined;
  createdByUser?: UserInfo | null | undefined;
  description?: string | null | undefined;
  /**
   * List of labels associated with the project.
   */
  labels?: Array<ProjectLabels> | undefined;
};

/** @internal */
export const ProjectItem$inboundSchema: z.ZodMiniType<ProjectItem, unknown> = z
  .pipe(
    z.object({
      id: types.string(),
      permissions: types.optional(z.array(Permission$inboundSchema)),
      name: types.string(),
      created_at: types.date(),
      updated_at: types.date(),
      bookmark: z._default(types.boolean(), false),
      num_logstreams: z.optional(z.nullable(types.number())),
      num_experiments: z.optional(z.nullable(types.number())),
      created_by_user: z.optional(z.nullable(UserInfo$inboundSchema)),
      description: z.optional(z.nullable(types.string())),
      labels: types.optional(z.array(ProjectLabels$inboundSchema)),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "updated_at": "updatedAt",
        "num_logstreams": "numLogstreams",
        "num_experiments": "numExperiments",
        "created_by_user": "createdByUser",
      });
    }),
  );

export function projectItemFromJSON(
  jsonString: string,
): SafeParseResult<ProjectItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ProjectItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ProjectItem' from JSON`,
  );
}
