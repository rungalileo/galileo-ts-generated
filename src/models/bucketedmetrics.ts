/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: ccf1f496e965
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type BucketedMetrics = {
  startBucketTime: Date;
  endBucketTime: Date;
  [additionalProperties: string]: unknown;
};

/** @internal */
export const BucketedMetrics$inboundSchema: z.ZodMiniType<
  BucketedMetrics,
  unknown
> = z.pipe(
  z.catchall(
    z.object({
      start_bucket_time: types.date(),
      end_bucket_time: types.date(),
    }),
    z.any(),
  ),
  z.transform((v) => {
    return remap$(v, {
      "start_bucket_time": "startBucketTime",
      "end_bucket_time": "endBucketTime",
    });
  }),
);

export function bucketedMetricsFromJSON(
  jsonString: string,
): SafeParseResult<BucketedMetrics, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BucketedMetrics$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BucketedMetrics' from JSON`,
  );
}
