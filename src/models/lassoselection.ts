/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: df788ebcd6a9
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Representation of a lasso selection (used by embeddings)
 *
 * @remarks
 *
 * x and y correspond to the cursor movement while tracing the lasso. This is natively provided by plotly when creating
 * a lasso selection, for example
 */
export type LassoSelection = {
  x: Array<number>;
  y: Array<number>;
};

/** @internal */
export const LassoSelection$inboundSchema: z.ZodMiniType<
  LassoSelection,
  unknown
> = z.object({
  x: z.array(types.number()),
  y: z.array(types.number()),
});
/** @internal */
export type LassoSelection$Outbound = {
  x: Array<number>;
  y: Array<number>;
};

/** @internal */
export const LassoSelection$outboundSchema: z.ZodMiniType<
  LassoSelection$Outbound,
  LassoSelection
> = z.object({
  x: z.array(z.number()),
  y: z.array(z.number()),
});

export function lassoSelectionToJSON(lassoSelection: LassoSelection): string {
  return JSON.stringify(LassoSelection$outboundSchema.parse(lassoSelection));
}
export function lassoSelectionFromJSON(
  jsonString: string,
): SafeParseResult<LassoSelection, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LassoSelection$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LassoSelection' from JSON`,
  );
}
