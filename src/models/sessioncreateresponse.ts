/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 288466a20c9f
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type SessionCreateResponse = {
  /**
   * Session id associated with the session.
   */
  id: string;
  /**
   * Name of the session.
   */
  name: string | null;
  /**
   * Project id associated with the session.
   */
  projectId: string;
  /**
   * Project name associated with the session.
   */
  projectName: string;
  /**
   * Id of the previous session.
   */
  previousSessionId?: string | null | undefined;
  /**
   * External id of the session.
   */
  externalId?: string | null | undefined;
};

/** @internal */
export const SessionCreateResponse$inboundSchema: z.ZodMiniType<
  SessionCreateResponse,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    name: types.nullable(types.string()),
    project_id: types.string(),
    project_name: types.string(),
    previous_session_id: z.optional(z.nullable(types.string())),
    external_id: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "project_id": "projectId",
      "project_name": "projectName",
      "previous_session_id": "previousSessionId",
      "external_id": "externalId",
    });
  }),
);

export function sessionCreateResponseFromJSON(
  jsonString: string,
): SafeParseResult<SessionCreateResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SessionCreateResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SessionCreateResponse' from JSON`,
  );
}
