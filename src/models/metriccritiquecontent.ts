/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type MetricCritiqueContent = {
  critique: string;
  intendedValue: boolean;
  originalExplanation: string;
};

/** @internal */
export const MetricCritiqueContent$inboundSchema: z.ZodMiniType<
  MetricCritiqueContent,
  unknown
> = z.pipe(
  z.object({
    critique: types.string(),
    intended_value: types.boolean(),
    original_explanation: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "intended_value": "intendedValue",
      "original_explanation": "originalExplanation",
    });
  }),
);

export function metricCritiqueContentFromJSON(
  jsonString: string,
): SafeParseResult<MetricCritiqueContent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetricCritiqueContent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetricCritiqueContent' from JSON`,
  );
}
