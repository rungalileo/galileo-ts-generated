/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f6724862c77e
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  SubscriptionConfig,
  SubscriptionConfig$inboundSchema,
  SubscriptionConfig$Outbound,
  SubscriptionConfig$outboundSchema,
} from "./subscriptionconfig.js";

export type OverrideAction = {
  type: "OVERRIDE";
  /**
   * List of subscriptions to send a notification to when this action is applied and the ruleset status matches any of the configured statuses.
   */
  subscriptions?: Array<SubscriptionConfig> | undefined;
  /**
   * List of choices to override the response with. If there are multiple choices, one will be chosen at random when applying this action.
   */
  choices: Array<string>;
};

/** @internal */
export const OverrideAction$inboundSchema: z.ZodMiniType<
  OverrideAction,
  unknown
> = z.object({
  type: types.literal("OVERRIDE"),
  subscriptions: types.optional(z.array(SubscriptionConfig$inboundSchema)),
  choices: z.array(types.string()),
});
/** @internal */
export type OverrideAction$Outbound = {
  type: "OVERRIDE";
  subscriptions?: Array<SubscriptionConfig$Outbound> | undefined;
  choices: Array<string>;
};

/** @internal */
export const OverrideAction$outboundSchema: z.ZodMiniType<
  OverrideAction$Outbound,
  OverrideAction
> = z.object({
  type: z.literal("OVERRIDE"),
  subscriptions: z.optional(z.array(SubscriptionConfig$outboundSchema)),
  choices: z.array(z.string()),
});

export function overrideActionToJSON(overrideAction: OverrideAction): string {
  return JSON.stringify(OverrideAction$outboundSchema.parse(overrideAction));
}
export function overrideActionFromJSON(
  jsonString: string,
): SafeParseResult<OverrideAction, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => OverrideAction$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'OverrideAction' from JSON`,
  );
}
