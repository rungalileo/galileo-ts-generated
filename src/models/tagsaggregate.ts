/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type TagsAggregate = {
  feedbackType: "tags";
  counts: { [k: string]: number };
  unratedCount: number;
};

/** @internal */
export const TagsAggregate$inboundSchema: z.ZodMiniType<
  TagsAggregate,
  unknown
> = z.pipe(
  z.object({
    feedback_type: types.literal("tags"),
    counts: z.record(z.string(), types.number()),
    unrated_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feedback_type": "feedbackType",
      "unrated_count": "unratedCount",
    });
  }),
);

export function tagsAggregateFromJSON(
  jsonString: string,
): SafeParseResult<TagsAggregate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TagsAggregate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TagsAggregate' from JSON`,
  );
}
