/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 4a86bd768045
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type LogSpansIngestResponse = {
  /**
   * Log stream id associated with the traces.
   */
  logStreamId?: string | null | undefined;
  /**
   * Experiment id associated with the traces.
   */
  experimentId?: string | null | undefined;
  /**
   * Metrics testing id associated with the traces.
   */
  metricsTestingId?: string | null | undefined;
  /**
   * Project id associated with the traces.
   */
  projectId: string;
  /**
   * Project name associated with the traces.
   */
  projectName: string;
  /**
   * Session id associated with the traces.
   */
  sessionId?: string | null | undefined;
  /**
   * Total number of records ingested
   */
  recordsCount: number;
  /**
   * Trace id associated with the spans.
   */
  traceId: string;
  /**
   * Parent trace or span id.
   */
  parentId: string;
};

/** @internal */
export const LogSpansIngestResponse$inboundSchema: z.ZodMiniType<
  LogSpansIngestResponse,
  unknown
> = z.pipe(
  z.object({
    log_stream_id: z.optional(z.nullable(types.string())),
    experiment_id: z.optional(z.nullable(types.string())),
    metrics_testing_id: z.optional(z.nullable(types.string())),
    project_id: types.string(),
    project_name: types.string(),
    session_id: z.optional(z.nullable(types.string())),
    records_count: types.number(),
    trace_id: types.string(),
    parent_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "log_stream_id": "logStreamId",
      "experiment_id": "experimentId",
      "metrics_testing_id": "metricsTestingId",
      "project_id": "projectId",
      "project_name": "projectName",
      "session_id": "sessionId",
      "records_count": "recordsCount",
      "trace_id": "traceId",
      "parent_id": "parentId",
    });
  }),
);

export function logSpansIngestResponseFromJSON(
  jsonString: string,
): SafeParseResult<LogSpansIngestResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LogSpansIngestResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LogSpansIngestResponse' from JSON`,
  );
}
