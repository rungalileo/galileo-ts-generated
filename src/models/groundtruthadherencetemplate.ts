/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0b4692cd7d92
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  FewShotExample,
  FewShotExample$inboundSchema,
  FewShotExample$Outbound,
  FewShotExample$outboundSchema,
} from "./fewshotexample.js";

export type GroundTruthAdherenceTemplate = {
  metricSystemPrompt?: string | undefined;
  metricDescription?: string | undefined;
  valueFieldName?: string | undefined;
  /**
   * Field name to look for in the chainpoll response, for the explanation.
   */
  explanationFieldName?: string | undefined;
  template?: string | undefined;
  /**
   * Few-shot examples for the metric.
   */
  metricFewShotExamples?: Array<FewShotExample> | undefined;
  /**
   * Response schema for the output
   */
  responseSchema?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const GroundTruthAdherenceTemplate$inboundSchema: z.ZodMiniType<
  GroundTruthAdherenceTemplate,
  unknown
> = z.pipe(
  z.object({
    metric_system_prompt: z._default(
      types.string(),
      "I will give you two different texts, called the \"ground truth\" and the \"response.\"\n\nRead both texts, then tell me whether they are \"equivalent,\" in the sense that they basically mean the same thing.\n\nKeep the following guidelines in mind.\n\n- Two texts can be equivalent if they use different phrasing, as long as the phrasing doesn't affect meaning.\n- Two texts can be equivalent if there are _slight_ differences in meaning that wouldn't affect the conclusions that a reasonable reader would draw upon reading them.\n- Imagine that you are grading a free-response exam.  The ground truth given in the answer key for an exam question, and the response is a student's answer to the same question. If you would give the student full marks for this question, that means the two texts are equivalent. If you wouldn't, that means the two texts are not equivalent.\n\nRespond in the following JSON format:\n\n```\n{{\n    \"explanation\": string,\n    \"equivalent\": boolean\n}}\n```\n\n\"explanation\": A step-by-step breakdown of the similarities and differences between the text. For each difference you note (if any), consider why the difference might or might not make the texts non-equivalent, note down your reasoning clearly and explicitly, and ultimately draw a conclusion about whether that difference makes the text non-equivalent.\n\n\"equivalent\": `true` if the texts are equivalent in the sense given above, `false` if they are non-equivalent.\n\nYou must respond with valid JSON.",
    ),
    metric_description: z._default(
      types.string(),
      "This metric computes whether a response from a large language model matches a provided ground truth text.",
    ),
    value_field_name: z._default(types.string(), "equivalent"),
    explanation_field_name: z._default(types.string(), "explanation"),
    template: z._default(
      types.string(),
      "Ground truth:\n\n```\n{ground_truth}\n```\n\nResponse:\n\n```\n{response}\n```",
    ),
    metric_few_shot_examples: types.optional(
      z.array(FewShotExample$inboundSchema),
    ),
    response_schema: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metric_system_prompt": "metricSystemPrompt",
      "metric_description": "metricDescription",
      "value_field_name": "valueFieldName",
      "explanation_field_name": "explanationFieldName",
      "metric_few_shot_examples": "metricFewShotExamples",
      "response_schema": "responseSchema",
    });
  }),
);
/** @internal */
export type GroundTruthAdherenceTemplate$Outbound = {
  metric_system_prompt: string;
  metric_description: string;
  value_field_name: string;
  explanation_field_name: string;
  template: string;
  metric_few_shot_examples?: Array<FewShotExample$Outbound> | undefined;
  response_schema?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const GroundTruthAdherenceTemplate$outboundSchema: z.ZodMiniType<
  GroundTruthAdherenceTemplate$Outbound,
  GroundTruthAdherenceTemplate
> = z.pipe(
  z.object({
    metricSystemPrompt: z._default(
      z.string(),
      "I will give you two different texts, called the \"ground truth\" and the \"response.\"\n\nRead both texts, then tell me whether they are \"equivalent,\" in the sense that they basically mean the same thing.\n\nKeep the following guidelines in mind.\n\n- Two texts can be equivalent if they use different phrasing, as long as the phrasing doesn't affect meaning.\n- Two texts can be equivalent if there are _slight_ differences in meaning that wouldn't affect the conclusions that a reasonable reader would draw upon reading them.\n- Imagine that you are grading a free-response exam.  The ground truth given in the answer key for an exam question, and the response is a student's answer to the same question. If you would give the student full marks for this question, that means the two texts are equivalent. If you wouldn't, that means the two texts are not equivalent.\n\nRespond in the following JSON format:\n\n```\n{{\n    \"explanation\": string,\n    \"equivalent\": boolean\n}}\n```\n\n\"explanation\": A step-by-step breakdown of the similarities and differences between the text. For each difference you note (if any), consider why the difference might or might not make the texts non-equivalent, note down your reasoning clearly and explicitly, and ultimately draw a conclusion about whether that difference makes the text non-equivalent.\n\n\"equivalent\": `true` if the texts are equivalent in the sense given above, `false` if they are non-equivalent.\n\nYou must respond with valid JSON.",
    ),
    metricDescription: z._default(
      z.string(),
      "This metric computes whether a response from a large language model matches a provided ground truth text.",
    ),
    valueFieldName: z._default(z.string(), "equivalent"),
    explanationFieldName: z._default(z.string(), "explanation"),
    template: z._default(
      z.string(),
      "Ground truth:\n\n```\n{ground_truth}\n```\n\nResponse:\n\n```\n{response}\n```",
    ),
    metricFewShotExamples: z.optional(z.array(FewShotExample$outboundSchema)),
    responseSchema: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      metricSystemPrompt: "metric_system_prompt",
      metricDescription: "metric_description",
      valueFieldName: "value_field_name",
      explanationFieldName: "explanation_field_name",
      metricFewShotExamples: "metric_few_shot_examples",
      responseSchema: "response_schema",
    });
  }),
);

export function groundTruthAdherenceTemplateToJSON(
  groundTruthAdherenceTemplate: GroundTruthAdherenceTemplate,
): string {
  return JSON.stringify(
    GroundTruthAdherenceTemplate$outboundSchema.parse(
      groundTruthAdherenceTemplate,
    ),
  );
}
export function groundTruthAdherenceTemplateFromJSON(
  jsonString: string,
): SafeParseResult<GroundTruthAdherenceTemplate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GroundTruthAdherenceTemplate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GroundTruthAdherenceTemplate' from JSON`,
  );
}
