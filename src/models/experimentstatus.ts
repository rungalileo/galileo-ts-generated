/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: bc1c43352e43
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ExperimentPhaseStatus,
  ExperimentPhaseStatus$inboundSchema,
} from "./experimentphasestatus.js";

export type ExperimentStatus = {
  logGeneration?: ExperimentPhaseStatus | undefined;
};

/** @internal */
export const ExperimentStatus$inboundSchema: z.ZodMiniType<
  ExperimentStatus,
  unknown
> = z.pipe(
  z.object({
    log_generation: types.optional(ExperimentPhaseStatus$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "log_generation": "logGeneration",
    });
  }),
);

export function experimentStatusFromJSON(
  jsonString: string,
): SafeParseResult<ExperimentStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExperimentStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExperimentStatus' from JSON`,
  );
}
