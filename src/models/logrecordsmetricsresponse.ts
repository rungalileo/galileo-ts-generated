/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import {
  BucketedMetrics,
  BucketedMetrics$inboundSchema,
} from "./bucketedmetrics.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { StandardError, StandardError$inboundSchema } from "./standarderror.js";

export type AggregateMetrics = number | number;

export type LogRecordsMetricsResponse = {
  groupByColumns: Array<string>;
  aggregateMetrics: { [k: string]: number | number };
  bucketedMetrics: { [k: string]: Array<BucketedMetrics> };
  /**
   * Whether any EMS error codes were encountered in the queried metrics
   */
  emsCapturedError: boolean;
  /**
   * Structured EMS errors for each error code encountered, keyed by code
   */
  standardErrors?: { [k: string]: StandardError } | null | undefined;
};

/** @internal */
export const AggregateMetrics$inboundSchema: z.ZodMiniType<
  AggregateMetrics,
  unknown
> = smartUnion([types.number(), types.number()]);

export function aggregateMetricsFromJSON(
  jsonString: string,
): SafeParseResult<AggregateMetrics, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AggregateMetrics$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AggregateMetrics' from JSON`,
  );
}

/** @internal */
export const LogRecordsMetricsResponse$inboundSchema: z.ZodMiniType<
  LogRecordsMetricsResponse,
  unknown
> = z.pipe(
  z.object({
    group_by_columns: z.array(types.string()),
    aggregate_metrics: z.record(
      z.string(),
      smartUnion([types.number(), types.number()]),
    ),
    bucketed_metrics: z.record(
      z.string(),
      z.array(BucketedMetrics$inboundSchema),
    ),
    ems_captured_error: z._default(types.boolean(), false),
    standard_errors: z.optional(
      z.nullable(z.record(z.string(), StandardError$inboundSchema)),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "group_by_columns": "groupByColumns",
      "aggregate_metrics": "aggregateMetrics",
      "bucketed_metrics": "bucketedMetrics",
      "ems_captured_error": "emsCapturedError",
      "standard_errors": "standardErrors",
    });
  }),
);

export function logRecordsMetricsResponseFromJSON(
  jsonString: string,
): SafeParseResult<LogRecordsMetricsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LogRecordsMetricsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LogRecordsMetricsResponse' from JSON`,
  );
}
