/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AnnotationStarAggregate = {
  annotationType: "star";
  average: number;
  counts: { [k: string]: number };
  unratedCount: number;
};

/** @internal */
export const AnnotationStarAggregate$inboundSchema: z.ZodMiniType<
  AnnotationStarAggregate,
  unknown
> = z.pipe(
  z.object({
    annotation_type: types.literal("star"),
    average: types.number(),
    counts: z.record(z.string(), types.number()),
    unrated_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
      "unrated_count": "unratedCount",
    });
  }),
);

export function annotationStarAggregateFromJSON(
  jsonString: string,
): SafeParseResult<AnnotationStarAggregate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnotationStarAggregate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnotationStarAggregate' from JSON`,
  );
}
