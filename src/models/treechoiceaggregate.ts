/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e95bb612c318
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type TreeChoiceAggregate = {
  feedbackType: "tree_choice";
  counts: { [k: string]: number };
  unratedCount: number;
};

/** @internal */
export const TreeChoiceAggregate$inboundSchema: z.ZodMiniType<
  TreeChoiceAggregate,
  unknown
> = z.pipe(
  z.object({
    feedback_type: types.literal("tree_choice"),
    counts: z.record(z.string(), types.number()),
    unrated_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feedback_type": "feedbackType",
      "unrated_count": "unratedCount",
    });
  }),
);

export function treeChoiceAggregateFromJSON(
  jsonString: string,
): SafeParseResult<TreeChoiceAggregate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TreeChoiceAggregate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TreeChoiceAggregate' from JSON`,
  );
}
