/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 02d0185002a6
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Minimal log stream representation (id and name only).
 */
export type LogStreamInfo = {
  id: string;
  name: string;
};

/** @internal */
export const LogStreamInfo$inboundSchema: z.ZodMiniType<
  LogStreamInfo,
  unknown
> = z.object({
  id: types.string(),
  name: types.string(),
});

export function logStreamInfoFromJSON(
  jsonString: string,
): SafeParseResult<LogStreamInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LogStreamInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LogStreamInfo' from JSON`,
  );
}
