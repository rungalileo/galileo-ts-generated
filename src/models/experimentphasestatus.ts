/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0900cd2db78a
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ExperimentPhaseStatus = {
  /**
   * Progress percentage from 0.0 to 1.0
   */
  progressPercent: number;
};

/** @internal */
export const ExperimentPhaseStatus$inboundSchema: z.ZodMiniType<
  ExperimentPhaseStatus,
  unknown
> = z.pipe(
  z.object({
    progress_percent: z._default(types.number(), 0),
  }),
  z.transform((v) => {
    return remap$(v, {
      "progress_percent": "progressPercent",
    });
  }),
);

export function experimentPhaseStatusFromJSON(
  jsonString: string,
): SafeParseResult<ExperimentPhaseStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExperimentPhaseStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExperimentPhaseStatus' from JSON`,
  );
}
