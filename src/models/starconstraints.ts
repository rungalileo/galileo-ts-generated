/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 2d87774c1495
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type StarConstraints = {
  annotationType: "star";
};

/** @internal */
export const StarConstraints$inboundSchema: z.ZodMiniType<
  StarConstraints,
  unknown
> = z.pipe(
  z.object({
    annotation_type: types.literal("star"),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
    });
  }),
);
/** @internal */
export type StarConstraints$Outbound = {
  annotation_type: "star";
};

/** @internal */
export const StarConstraints$outboundSchema: z.ZodMiniType<
  StarConstraints$Outbound,
  StarConstraints
> = z.pipe(
  z.object({
    annotationType: z.literal("star"),
  }),
  z.transform((v) => {
    return remap$(v, {
      annotationType: "annotation_type",
    });
  }),
);

export function starConstraintsToJSON(
  starConstraints: StarConstraints,
): string {
  return JSON.stringify(StarConstraints$outboundSchema.parse(starConstraints));
}
export function starConstraintsFromJSON(
  jsonString: string,
): SafeParseResult<StarConstraints, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StarConstraints$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StarConstraints' from JSON`,
  );
}
