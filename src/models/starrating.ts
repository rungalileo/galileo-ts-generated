/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type StarRating = {
  feedbackType: "star";
  value: number;
};

/** @internal */
export const StarRating$inboundSchema: z.ZodMiniType<StarRating, unknown> = z
  .pipe(
    z.object({
      feedback_type: types.literal("star"),
      value: types.number(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "feedback_type": "feedbackType",
      });
    }),
  );

export function starRatingFromJSON(
  jsonString: string,
): SafeParseResult<StarRating, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StarRating$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StarRating' from JSON`,
  );
}
