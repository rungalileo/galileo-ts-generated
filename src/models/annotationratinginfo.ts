/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 31da916af07f
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import {
  AnnotationType,
  AnnotationType$inboundSchema,
} from "./annotationtype.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AnnotationRatingInfoValue =
  | boolean
  | number
  | string
  | Array<string>;

export type AnnotationRatingInfo = {
  annotationType: AnnotationType;
  value: boolean | number | string | Array<string>;
  explanation: string | null;
};

/** @internal */
export const AnnotationRatingInfoValue$inboundSchema: z.ZodMiniType<
  AnnotationRatingInfoValue,
  unknown
> = smartUnion([
  types.boolean(),
  types.number(),
  types.string(),
  z.array(types.string()),
]);

export function annotationRatingInfoValueFromJSON(
  jsonString: string,
): SafeParseResult<AnnotationRatingInfoValue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnotationRatingInfoValue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnotationRatingInfoValue' from JSON`,
  );
}

/** @internal */
export const AnnotationRatingInfo$inboundSchema: z.ZodMiniType<
  AnnotationRatingInfo,
  unknown
> = z.pipe(
  z.object({
    annotation_type: AnnotationType$inboundSchema,
    value: smartUnion([
      types.boolean(),
      types.number(),
      types.string(),
      z.array(types.string()),
    ]),
    explanation: types.nullable(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
    });
  }),
);

export function annotationRatingInfoFromJSON(
  jsonString: string,
): SafeParseResult<AnnotationRatingInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnotationRatingInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnotationRatingInfo' from JSON`,
  );
}
