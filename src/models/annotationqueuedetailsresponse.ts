/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 518a0ea35d55
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  AnnotationAggregate,
  AnnotationAggregate$inboundSchema,
} from "./annotationaggregate.js";
import {
  AnnotationAgreementAggregate,
  AnnotationAgreementAggregate$inboundSchema,
} from "./annotationagreementaggregate.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AnnotationQueueDetailsResponse = {
  /**
   * Count of queue logs that have a rating for every queue template from each annotation-capable collaborator with track_progress enabled.
   */
  numLogsFullyAnnotated: number;
  /**
   * Queue-wide aggregates keyed by annotation template UUID. Null when the caller cannot view queue-wide aggregates.
   */
  annotationAggregates?:
    | { [k: string]: AnnotationAggregate }
    | null
    | undefined;
  /**
   * Per-user aggregates keyed by annotation-capable collaborator UUID, then annotation template UUID. Null when the caller cannot view all per-user aggregates for the queue.
   */
  annotationAggregatesByAnnotator?:
    | { [k: string]: { [k: string]: AnnotationAggregate } }
    | null
    | undefined;
  /**
   * Queue-wide aggregate of record-level overall annotator agreement. Null when the caller cannot view queue-wide aggregates.
   */
  overallAnnotationAgreement?: AnnotationAgreementAggregate | null | undefined;
};

/** @internal */
export const AnnotationQueueDetailsResponse$inboundSchema: z.ZodMiniType<
  AnnotationQueueDetailsResponse,
  unknown
> = z.pipe(
  z.object({
    num_logs_fully_annotated: z._default(types.number(), 0),
    annotation_aggregates: z.optional(
      z.nullable(z.record(z.string(), AnnotationAggregate$inboundSchema)),
    ),
    annotation_aggregates_by_annotator: z.optional(
      z.nullable(
        z.record(
          z.string(),
          z.record(z.string(), AnnotationAggregate$inboundSchema),
        ),
      ),
    ),
    overall_annotation_agreement: z.optional(
      z.nullable(AnnotationAgreementAggregate$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "num_logs_fully_annotated": "numLogsFullyAnnotated",
      "annotation_aggregates": "annotationAggregates",
      "annotation_aggregates_by_annotator": "annotationAggregatesByAnnotator",
      "overall_annotation_agreement": "overallAnnotationAgreement",
    });
  }),
);

export function annotationQueueDetailsResponseFromJSON(
  jsonString: string,
): SafeParseResult<AnnotationQueueDetailsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnotationQueueDetailsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnotationQueueDetailsResponse' from JSON`,
  );
}
