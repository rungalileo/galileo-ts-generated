/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ExperimentResponse,
  ExperimentResponse$inboundSchema,
} from "./experimentresponse.js";

export type ListExperimentResponse = {
  startingToken: number;
  limit: number;
  paginated: boolean;
  nextStartingToken?: number | null | undefined;
  experiments?: Array<ExperimentResponse> | undefined;
};

/** @internal */
export const ListExperimentResponse$inboundSchema: z.ZodMiniType<
  ListExperimentResponse,
  unknown
> = z.pipe(
  z.object({
    starting_token: z._default(types.number(), 0),
    limit: z._default(types.number(), 100),
    paginated: z._default(types.boolean(), false),
    next_starting_token: z.optional(z.nullable(types.number())),
    experiments: types.optional(z.array(ExperimentResponse$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "starting_token": "startingToken",
      "next_starting_token": "nextStartingToken",
    });
  }),
);

export function listExperimentResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListExperimentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListExperimentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListExperimentResponse' from JSON`,
  );
}
