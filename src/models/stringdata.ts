/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type StringData = {
  inputStrings: Array<string>;
};

/** @internal */
export type StringData$Outbound = {
  input_strings: Array<string>;
};

/** @internal */
export const StringData$outboundSchema: z.ZodMiniType<
  StringData$Outbound,
  StringData
> = z.pipe(
  z.object({
    inputStrings: z.array(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      inputStrings: "input_strings",
    });
  }),
);

export function stringDataToJSON(stringData: StringData): string {
  return JSON.stringify(StringData$outboundSchema.parse(stringData));
}
