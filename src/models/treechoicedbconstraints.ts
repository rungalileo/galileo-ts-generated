/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e7b587918ffa
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  TreeChoiceNode,
  TreeChoiceNode$inboundSchema,
} from "./treechoicenode.js";

export type TreeChoiceDBConstraints = {
  annotationType: "tree_choice";
  choicesTree: Array<TreeChoiceNode>;
  choicesTreeYaml: string;
};

/** @internal */
export const TreeChoiceDBConstraints$inboundSchema: z.ZodMiniType<
  TreeChoiceDBConstraints,
  unknown
> = z.pipe(
  z.object({
    annotation_type: types.literal("tree_choice"),
    choices_tree: z.array(TreeChoiceNode$inboundSchema),
    choices_tree_yaml: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
      "choices_tree": "choicesTree",
      "choices_tree_yaml": "choicesTreeYaml",
    });
  }),
);

export function treeChoiceDBConstraintsFromJSON(
  jsonString: string,
): SafeParseResult<TreeChoiceDBConstraints, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TreeChoiceDBConstraints$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TreeChoiceDBConstraints' from JSON`,
  );
}
