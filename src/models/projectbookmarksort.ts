/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 20be9bdeb5e8
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type ProjectBookmarkSort = {
  name: "bookmark";
  ascending?: boolean | undefined;
  sortType?: "custom" | undefined;
};

/** @internal */
export type ProjectBookmarkSort$Outbound = {
  name: "bookmark";
  ascending: boolean;
  sort_type: "custom";
};

/** @internal */
export const ProjectBookmarkSort$outboundSchema: z.ZodMiniType<
  ProjectBookmarkSort$Outbound,
  ProjectBookmarkSort
> = z.pipe(
  z.object({
    name: z.literal("bookmark"),
    ascending: z._default(z.boolean(), true),
    sortType: z._default(z.literal("custom"), "custom" as const),
  }),
  z.transform((v) => {
    return remap$(v, {
      sortType: "sort_type",
    });
  }),
);

export function projectBookmarkSortToJSON(
  projectBookmarkSort: ProjectBookmarkSort,
): string {
  return JSON.stringify(
    ProjectBookmarkSort$outboundSchema.parse(projectBookmarkSort),
  );
}
