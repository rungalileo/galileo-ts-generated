/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f07e0680dbb6
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type TaskResourceLimits = {
  cpuTime?: number | undefined;
  memoryMb?: number | undefined;
};

/** @internal */
export const TaskResourceLimits$inboundSchema: z.ZodMiniType<
  TaskResourceLimits,
  unknown
> = z.pipe(
  z.object({
    cpu_time: z._default(types.number(), 216),
    memory_mb: z._default(types.number(), 160),
  }),
  z.transform((v) => {
    return remap$(v, {
      "cpu_time": "cpuTime",
      "memory_mb": "memoryMb",
    });
  }),
);
/** @internal */
export type TaskResourceLimits$Outbound = {
  cpu_time: number;
  memory_mb: number;
};

/** @internal */
export const TaskResourceLimits$outboundSchema: z.ZodMiniType<
  TaskResourceLimits$Outbound,
  TaskResourceLimits
> = z.pipe(
  z.object({
    cpuTime: z._default(z.int(), 216),
    memoryMb: z._default(z.int(), 160),
  }),
  z.transform((v) => {
    return remap$(v, {
      cpuTime: "cpu_time",
      memoryMb: "memory_mb",
    });
  }),
);

export function taskResourceLimitsToJSON(
  taskResourceLimits: TaskResourceLimits,
): string {
  return JSON.stringify(
    TaskResourceLimits$outboundSchema.parse(taskResourceLimits),
  );
}
export function taskResourceLimitsFromJSON(
  jsonString: string,
): SafeParseResult<TaskResourceLimits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaskResourceLimits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaskResourceLimits' from JSON`,
  );
}
