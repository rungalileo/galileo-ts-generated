/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 612014d15cfd
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ProjectIntegrationCosts,
  ProjectIntegrationCosts$inboundSchema,
} from "./projectintegrationcosts.js";

export type FeatureIntegrationCosts = {
  featureName: string;
  totalCost: number;
  projects?: Array<ProjectIntegrationCosts> | undefined;
};

/** @internal */
export const FeatureIntegrationCosts$inboundSchema: z.ZodMiniType<
  FeatureIntegrationCosts,
  unknown
> = z.pipe(
  z.object({
    feature_name: types.string(),
    total_cost: z._default(types.number(), 0),
    projects: types.optional(z.array(ProjectIntegrationCosts$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feature_name": "featureName",
      "total_cost": "totalCost",
    });
  }),
);

export function featureIntegrationCostsFromJSON(
  jsonString: string,
): SafeParseResult<FeatureIntegrationCosts, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FeatureIntegrationCosts$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FeatureIntegrationCosts' from JSON`,
  );
}
