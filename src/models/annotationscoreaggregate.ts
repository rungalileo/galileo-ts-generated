/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cdfa7aa7bcf4
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AnnotationScoreAggregate = {
  annotationType: "score";
  average: number;
  unratedCount: number;
};

/** @internal */
export const AnnotationScoreAggregate$inboundSchema: z.ZodMiniType<
  AnnotationScoreAggregate,
  unknown
> = z.pipe(
  z.object({
    annotation_type: types.literal("score"),
    average: types.number(),
    unrated_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
      "unrated_count": "unratedCount",
    });
  }),
);

export function annotationScoreAggregateFromJSON(
  jsonString: string,
): SafeParseResult<AnnotationScoreAggregate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnotationScoreAggregate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnotationScoreAggregate' from JSON`,
  );
}
