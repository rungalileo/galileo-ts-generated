/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 2a4e20575ba8
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ScoreBucket = {
  minInclusive: number;
  maxExclusive: number | null;
  count: number;
};

/** @internal */
export const ScoreBucket$inboundSchema: z.ZodMiniType<ScoreBucket, unknown> = z
  .pipe(
    z.object({
      min_inclusive: types.number(),
      max_exclusive: types.nullable(types.number()),
      count: types.number(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "min_inclusive": "minInclusive",
        "max_exclusive": "maxExclusive",
      });
    }),
  );

export function scoreBucketFromJSON(
  jsonString: string,
): SafeParseResult<ScoreBucket, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScoreBucket$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScoreBucket' from JSON`,
  );
}
