/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ExecutionStatus,
  ExecutionStatus$inboundSchema,
  ExecutionStatus$outboundSchema,
} from "./executionstatus.js";

export type SubscriptionConfig = {
  /**
   * List of statuses that will cause a notification to be sent to the configured URL.
   */
  statuses?: Array<ExecutionStatus> | undefined;
  /**
   * URL to send the event to. This can be a webhook URL, a message queue URL, an event bus or a custom endpoint that can receive an HTTP POST request.
   */
  url: string;
};

/** @internal */
export const SubscriptionConfig$inboundSchema: z.ZodMiniType<
  SubscriptionConfig,
  unknown
> = z.object({
  statuses: types.optional(z.array(ExecutionStatus$inboundSchema)),
  url: types.string(),
});
/** @internal */
export type SubscriptionConfig$Outbound = {
  statuses?: Array<string> | undefined;
  url: string;
};

/** @internal */
export const SubscriptionConfig$outboundSchema: z.ZodMiniType<
  SubscriptionConfig$Outbound,
  SubscriptionConfig
> = z.object({
  statuses: z.optional(z.array(ExecutionStatus$outboundSchema)),
  url: z.string(),
});

export function subscriptionConfigToJSON(
  subscriptionConfig: SubscriptionConfig,
): string {
  return JSON.stringify(
    SubscriptionConfig$outboundSchema.parse(subscriptionConfig),
  );
}
export function subscriptionConfigFromJSON(
  jsonString: string,
): SafeParseResult<SubscriptionConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SubscriptionConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SubscriptionConfig' from JSON`,
  );
}
