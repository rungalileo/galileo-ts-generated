/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5589e8c09a27
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  CategoricalColorConstraint,
  CategoricalColorConstraint$inboundSchema,
} from "./categoricalcolorconstraint.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Color picker configuration for multi-label metrics.
 *
 * @remarks
 *
 * Behaves the same as categorical but intended for metrics that produce
 * multiple labels. A category value must not appear in more than one constraint.
 *
 * Example:
 *     {
 *         "type": "multi_label",
 *         "constraints": [
 *             {"color": "green", "operator": "eq", "value": "relevant"},
 *             {"color": "yellow", "operator": "one_of", "value": ["partial", "related"]}
 *         ]
 *     }
 */
export type MetricColorPickerMultiLabel = {
  type: "multi_label";
  constraints: Array<CategoricalColorConstraint>;
};

/** @internal */
export const MetricColorPickerMultiLabel$inboundSchema: z.ZodMiniType<
  MetricColorPickerMultiLabel,
  unknown
> = z.object({
  type: types.literal("multi_label"),
  constraints: z.array(CategoricalColorConstraint$inboundSchema),
});

export function metricColorPickerMultiLabelFromJSON(
  jsonString: string,
): SafeParseResult<MetricColorPickerMultiLabel, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetricColorPickerMultiLabel$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetricColorPickerMultiLabel' from JSON`,
  );
}
