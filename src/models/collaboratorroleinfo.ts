/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  CollaboratorRole,
  CollaboratorRole$inboundSchema,
} from "./collaboratorrole.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type CollaboratorRoleInfo = {
  name: CollaboratorRole;
  displayName: string;
  description: string;
};

/** @internal */
export const CollaboratorRoleInfo$inboundSchema: z.ZodMiniType<
  CollaboratorRoleInfo,
  unknown
> = z.pipe(
  z.object({
    name: CollaboratorRole$inboundSchema,
    display_name: types.string(),
    description: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "display_name": "displayName",
    });
  }),
);

export function collaboratorRoleInfoFromJSON(
  jsonString: string,
): SafeParseResult<CollaboratorRoleInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CollaboratorRoleInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CollaboratorRoleInfo' from JSON`,
  );
}
