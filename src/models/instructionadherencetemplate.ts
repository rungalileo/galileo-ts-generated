/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  FewShotExample,
  FewShotExample$inboundSchema,
  FewShotExample$Outbound,
  FewShotExample$outboundSchema,
} from "./fewshotexample.js";

export type InstructionAdherenceTemplate = {
  metricSystemPrompt?: string | undefined;
  metricDescription?: string | undefined;
  valueFieldName?: string | undefined;
  /**
   * Field name to look for in the chainpoll response, for the explanation.
   */
  explanationFieldName?: string | undefined;
  template?: string | undefined;
  metricFewShotExamples?: Array<FewShotExample> | undefined;
  /**
   * Response schema for the output
   */
  responseSchema?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const InstructionAdherenceTemplate$inboundSchema: z.ZodMiniType<
  InstructionAdherenceTemplate,
  unknown
> = z.pipe(
  z.object({
    metric_system_prompt: z._default(
      types.string(),
      "The user will provide you with a prompt that was sent to a chatbot system, and the chatbot's latest response. Both will be provided as JSON strings.\n\nIn some cases, the prompt may be split up into multiple messages. If so, each message will begin with one of the following prefixes:\n\n- \"System: \"\n- \"Human: \"\n- \"AI: \"\n\nIf you see these prefixes, pay attention to them because they indicate where messages begin and end. Messages prefixed with \"System: \" contain system instructions which the chatbot should follow. Messages prefixed with \"Human: \" are user input. Messages prefixed with \"AI: \" are system responses to user input.\nIf you do not see these prefixes, treat the prompt as though it was a single user input message prefixed with \"Human: \".\n\nYour task is to determine whether the latest response from the chatbot is consistent with the instructions provided in the system prompt (if there is one) or in the first user message (if there is no system prompt).\n\nFocus only on the latest response and the instructions. Do not consider the chat history or any previous messages from the chatbot.\n\nThink step by step, and explain your reasoning carefully.\nState your observations first, before drawing any conclusions.\n\nRespond in the following JSON format:\n\n```\n{\n    \"explanation\": string,\n    \"is_consistent\": boolean\n}\n```\n\n\"explanation\": Your step-by-step reasoning process. List out the relevant instructions and explain whether the latest response adheres to each of them.\n\n\"is_consistent\": `true` if the latest response is consistent with the instructions, `false` otherwise.\n\nYou must respond with a valid JSON string.",
    ),
    metric_description: z._default(
      types.string(),
      "I have a chatbot application.\nMy system prompt contains a list of instructions for what the chatbot should and should not do in every interaction. I want a metric that checks whether the latest response from the chatbot is consistent with the instructions.\n\nThe metric should only evaluate the latest message (the response), not the chat history. It should return false only if the latest message violates one or more instructions. Violations earlier in the chat history should not affect whether the value is true or false. The value should only depend on whether the latest message was consistent with the instructions, considered in context. The metric should only consider instructions that are applicable to the latest message.",
    ),
    value_field_name: z._default(types.string(), "is_consistent"),
    explanation_field_name: z._default(types.string(), "explanation"),
    template: z._default(
      types.string(),
      "Prompt JSON:\n\n```\n{query_json}\n```\n\nResponse JSON:\n\n```\n{response_json}\n```",
    ),
    metric_few_shot_examples: types.optional(
      z.array(FewShotExample$inboundSchema),
    ),
    response_schema: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metric_system_prompt": "metricSystemPrompt",
      "metric_description": "metricDescription",
      "value_field_name": "valueFieldName",
      "explanation_field_name": "explanationFieldName",
      "metric_few_shot_examples": "metricFewShotExamples",
      "response_schema": "responseSchema",
    });
  }),
);
/** @internal */
export type InstructionAdherenceTemplate$Outbound = {
  metric_system_prompt: string;
  metric_description: string;
  value_field_name: string;
  explanation_field_name: string;
  template: string;
  metric_few_shot_examples?: Array<FewShotExample$Outbound> | undefined;
  response_schema?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const InstructionAdherenceTemplate$outboundSchema: z.ZodMiniType<
  InstructionAdherenceTemplate$Outbound,
  InstructionAdherenceTemplate
> = z.pipe(
  z.object({
    metricSystemPrompt: z._default(
      z.string(),
      "The user will provide you with a prompt that was sent to a chatbot system, and the chatbot's latest response. Both will be provided as JSON strings.\n\nIn some cases, the prompt may be split up into multiple messages. If so, each message will begin with one of the following prefixes:\n\n- \"System: \"\n- \"Human: \"\n- \"AI: \"\n\nIf you see these prefixes, pay attention to them because they indicate where messages begin and end. Messages prefixed with \"System: \" contain system instructions which the chatbot should follow. Messages prefixed with \"Human: \" are user input. Messages prefixed with \"AI: \" are system responses to user input.\nIf you do not see these prefixes, treat the prompt as though it was a single user input message prefixed with \"Human: \".\n\nYour task is to determine whether the latest response from the chatbot is consistent with the instructions provided in the system prompt (if there is one) or in the first user message (if there is no system prompt).\n\nFocus only on the latest response and the instructions. Do not consider the chat history or any previous messages from the chatbot.\n\nThink step by step, and explain your reasoning carefully.\nState your observations first, before drawing any conclusions.\n\nRespond in the following JSON format:\n\n```\n{\n    \"explanation\": string,\n    \"is_consistent\": boolean\n}\n```\n\n\"explanation\": Your step-by-step reasoning process. List out the relevant instructions and explain whether the latest response adheres to each of them.\n\n\"is_consistent\": `true` if the latest response is consistent with the instructions, `false` otherwise.\n\nYou must respond with a valid JSON string.",
    ),
    metricDescription: z._default(
      z.string(),
      "I have a chatbot application.\nMy system prompt contains a list of instructions for what the chatbot should and should not do in every interaction. I want a metric that checks whether the latest response from the chatbot is consistent with the instructions.\n\nThe metric should only evaluate the latest message (the response), not the chat history. It should return false only if the latest message violates one or more instructions. Violations earlier in the chat history should not affect whether the value is true or false. The value should only depend on whether the latest message was consistent with the instructions, considered in context. The metric should only consider instructions that are applicable to the latest message.",
    ),
    valueFieldName: z._default(z.string(), "is_consistent"),
    explanationFieldName: z._default(z.string(), "explanation"),
    template: z._default(
      z.string(),
      "Prompt JSON:\n\n```\n{query_json}\n```\n\nResponse JSON:\n\n```\n{response_json}\n```",
    ),
    metricFewShotExamples: z.optional(z.array(FewShotExample$outboundSchema)),
    responseSchema: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      metricSystemPrompt: "metric_system_prompt",
      metricDescription: "metric_description",
      valueFieldName: "value_field_name",
      explanationFieldName: "explanation_field_name",
      metricFewShotExamples: "metric_few_shot_examples",
      responseSchema: "response_schema",
    });
  }),
);

export function instructionAdherenceTemplateToJSON(
  instructionAdherenceTemplate: InstructionAdherenceTemplate,
): string {
  return JSON.stringify(
    InstructionAdherenceTemplate$outboundSchema.parse(
      instructionAdherenceTemplate,
    ),
  );
}
export function instructionAdherenceTemplateFromJSON(
  jsonString: string,
): SafeParseResult<InstructionAdherenceTemplate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InstructionAdherenceTemplate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InstructionAdherenceTemplate' from JSON`,
  );
}
