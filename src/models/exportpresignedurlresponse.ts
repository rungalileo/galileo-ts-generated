/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 154cc7b4473b
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Response for an export written to object storage.
 */
export type ExportPresignedUrlResponse = {
  url: string;
  urlExpiresAt: Date;
  fileName: string;
  contentType: string;
};

/** @internal */
export const ExportPresignedUrlResponse$inboundSchema: z.ZodMiniType<
  ExportPresignedUrlResponse,
  unknown
> = z.pipe(
  z.object({
    url: types.string(),
    url_expires_at: types.date(),
    file_name: types.string(),
    content_type: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "url_expires_at": "urlExpiresAt",
      "file_name": "fileName",
      "content_type": "contentType",
    });
  }),
);

export function exportPresignedUrlResponseFromJSON(
  jsonString: string,
): SafeParseResult<ExportPresignedUrlResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExportPresignedUrlResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExportPresignedUrlResponse' from JSON`,
  );
}
