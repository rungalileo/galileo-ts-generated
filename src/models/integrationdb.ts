/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cc67d6b13a7e
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  IntegrationProvider,
  IntegrationProvider$inboundSchema,
} from "./integrationprovider.js";
import { Permission, Permission$inboundSchema } from "./permission.js";

export type IntegrationDB = {
  id: string;
  permissions?: Array<Permission> | undefined;
  name: string;
  provider: IntegrationProvider;
  createdAt: Date;
  updatedAt: Date;
  createdBy: string;
  isSelected: boolean;
  isDisabled: boolean;
};

/** @internal */
export const IntegrationDB$inboundSchema: z.ZodMiniType<
  IntegrationDB,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    permissions: types.optional(z.array(Permission$inboundSchema)),
    name: types.string(),
    provider: IntegrationProvider$inboundSchema,
    created_at: types.date(),
    updated_at: types.date(),
    created_by: types.string(),
    is_selected: z._default(types.boolean(), false),
    is_disabled: z._default(types.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "updated_at": "updatedAt",
      "created_by": "createdBy",
      "is_selected": "isSelected",
      "is_disabled": "isDisabled",
    });
  }),
);

export function integrationDBFromJSON(
  jsonString: string,
): SafeParseResult<IntegrationDB, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IntegrationDB$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IntegrationDB' from JSON`,
  );
}
