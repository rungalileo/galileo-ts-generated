/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: af2bf8623326
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  NumericColorConstraint,
  NumericColorConstraint$inboundSchema,
} from "./numericcolorconstraint.js";

/**
 * Color picker configuration for numeric metrics.
 *
 * @remarks
 *
 * Each constraint maps a numeric condition to a color. The UI uses these
 * constraints to color-code metric values (e.g. green for high scores,
 * red for low scores).
 *
 * Example:
 *     {
 *         "type": "numeric",
 *         "constraints": [
 *             {"color": "green", "operator": "gte", "value": 0.8},
 *             {"color": "yellow", "operator": "between", "value": [0.3, 0.8]},
 *             {"color": "red", "operator": "lt", "value": 0.3}
 *         ]
 *     }
 */
export type MetricColorPickerNumeric = {
  type: "numeric";
  constraints: Array<NumericColorConstraint>;
};

/** @internal */
export const MetricColorPickerNumeric$inboundSchema: z.ZodMiniType<
  MetricColorPickerNumeric,
  unknown
> = z.object({
  type: types.literal("numeric"),
  constraints: z.array(NumericColorConstraint$inboundSchema),
});

export function metricColorPickerNumericFromJSON(
  jsonString: string,
): SafeParseResult<MetricColorPickerNumeric, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetricColorPickerNumeric$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetricColorPickerNumeric' from JSON`,
  );
}
