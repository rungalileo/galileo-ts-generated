/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: cb254f8e45f9
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { StageType, StageType$inboundSchema } from "./stagetype.js";

export type StageDB = {
  /**
   * Name of the stage. Must be unique within the project.
   */
  name: string;
  /**
   * ID of the project to which this stage belongs.
   */
  projectId: string;
  /**
   * Optional human-readable description of the goals of this guardrail.
   */
  description?: string | null | undefined;
  type?: StageType | undefined;
  /**
   * Whether the action is enabled. If False, the action will not be applied.
   */
  paused: boolean;
  createdBy: string;
  id: string;
  version?: number | null | undefined;
};

/** @internal */
export const StageDB$inboundSchema: z.ZodMiniType<StageDB, unknown> = z.pipe(
  z.object({
    name: types.string(),
    project_id: types.string(),
    description: z.optional(z.nullable(types.string())),
    type: types.optional(StageType$inboundSchema),
    paused: z._default(types.boolean(), false),
    created_by: types.string(),
    id: types.string(),
    version: z.optional(z.nullable(types.number())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "project_id": "projectId",
      "created_by": "createdBy",
    });
  }),
);

export function stageDBFromJSON(
  jsonString: string,
): SafeParseResult<StageDB, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StageDB$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StageDB' from JSON`,
  );
}
