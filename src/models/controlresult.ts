/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: dec730036bcd
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  ControlAction,
  ControlAction$inboundSchema,
  ControlAction$outboundSchema,
} from "./controlaction.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ControlResult = {
  action: ControlAction;
  /**
   * Whether the control matched. False covers both non-match and error cases; use error_message to distinguish errors.
   */
  matched: boolean;
  /**
   * Confidence score reported by the control evaluation result.
   */
  confidence?: number | null | undefined;
  /**
   * Error text when control evaluation failed. This should be null for normal matches and non-matches.
   */
  errorMessage?: string | null | undefined;
};

/** @internal */
export const ControlResult$inboundSchema: z.ZodMiniType<
  ControlResult,
  unknown
> = z.pipe(
  z.object({
    action: ControlAction$inboundSchema,
    matched: types.boolean(),
    confidence: z.optional(z.nullable(types.number())),
    error_message: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "error_message": "errorMessage",
    });
  }),
);
/** @internal */
export type ControlResult$Outbound = {
  action: string;
  matched: boolean;
  confidence?: number | null | undefined;
  error_message?: string | null | undefined;
};

/** @internal */
export const ControlResult$outboundSchema: z.ZodMiniType<
  ControlResult$Outbound,
  ControlResult
> = z.pipe(
  z.object({
    action: ControlAction$outboundSchema,
    matched: z.boolean(),
    confidence: z.optional(z.nullable(z.number())),
    errorMessage: z.optional(z.nullable(z.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      errorMessage: "error_message",
    });
  }),
);

export function controlResultToJSON(controlResult: ControlResult): string {
  return JSON.stringify(ControlResult$outboundSchema.parse(controlResult));
}
export function controlResultFromJSON(
  jsonString: string,
): SafeParseResult<ControlResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ControlResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ControlResult' from JSON`,
  );
}
