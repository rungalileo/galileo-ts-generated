/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5d9b375ca16c
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  IntegrationCostsDataPoint,
  IntegrationCostsDataPoint$inboundSchema,
} from "./integrationcostsdatapoint.js";

export type ProjectIntegrationCosts = {
  projectId: string;
  projectName: string;
  totalCost: number;
  dataPoints?: Array<IntegrationCostsDataPoint> | undefined;
};

/** @internal */
export const ProjectIntegrationCosts$inboundSchema: z.ZodMiniType<
  ProjectIntegrationCosts,
  unknown
> = z.pipe(
  z.object({
    project_id: types.string(),
    project_name: types.string(),
    total_cost: z._default(types.number(), 0),
    data_points: types.optional(
      z.array(IntegrationCostsDataPoint$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "project_id": "projectId",
      "project_name": "projectName",
      "total_cost": "totalCost",
      "data_points": "dataPoints",
    });
  }),
);

export function projectIntegrationCostsFromJSON(
  jsonString: string,
): SafeParseResult<ProjectIntegrationCosts, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ProjectIntegrationCosts$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ProjectIntegrationCosts' from JSON`,
  );
}
