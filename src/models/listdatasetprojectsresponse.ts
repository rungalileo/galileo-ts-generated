/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  DatasetProject,
  DatasetProject$inboundSchema,
} from "./datasetproject.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ListDatasetProjectsResponse = {
  startingToken: number;
  limit: number;
  paginated: boolean;
  nextStartingToken?: number | null | undefined;
  projects?: Array<DatasetProject> | undefined;
};

/** @internal */
export const ListDatasetProjectsResponse$inboundSchema: z.ZodMiniType<
  ListDatasetProjectsResponse,
  unknown
> = z.pipe(
  z.object({
    starting_token: z._default(types.number(), 0),
    limit: z._default(types.number(), 100),
    paginated: z._default(types.boolean(), false),
    next_starting_token: z.optional(z.nullable(types.number())),
    projects: types.optional(z.array(DatasetProject$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "starting_token": "startingToken",
      "next_starting_token": "nextStartingToken",
    });
  }),
);

export function listDatasetProjectsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListDatasetProjectsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListDatasetProjectsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListDatasetProjectsResponse' from JSON`,
  );
}
