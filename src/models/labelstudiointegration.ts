/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f5600d0c80d1
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * The active labelstudio integration information.
 */
export type LabelStudioIntegration = {
  id?: string | null | undefined;
  name: "labelstudio";
  extra?: { [k: string]: any } | null | undefined;
  url: string;
  isEnterprise: boolean;
};

/** @internal */
export const LabelStudioIntegration$inboundSchema: z.ZodMiniType<
  LabelStudioIntegration,
  unknown
> = z.pipe(
  z.object({
    id: z.optional(z.nullable(types.string())),
    name: types.literal("labelstudio"),
    extra: z.optional(z.nullable(z.record(z.string(), z.any()))),
    url: types.string(),
    is_enterprise: types.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "is_enterprise": "isEnterprise",
    });
  }),
);

export function labelStudioIntegrationFromJSON(
  jsonString: string,
): SafeParseResult<LabelStudioIntegration, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LabelStudioIntegration$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LabelStudioIntegration' from JSON`,
  );
}
