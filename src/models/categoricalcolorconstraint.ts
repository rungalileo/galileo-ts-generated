/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 17b227d47fab
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { MetricColor, MetricColor$inboundSchema } from "./metriccolor.js";

export const CategoricalColorConstraintOperator = {
  Eq: "eq",
  OneOf: "one_of",
} as const;
export type CategoricalColorConstraintOperator = OpenEnum<
  typeof CategoricalColorConstraintOperator
>;

export type CategoricalColorConstraintValue = string | Array<string>;

/**
 * A color constraint for categorical or multi-label metric values.
 *
 * @remarks
 *
 * Assigns a color when a categorical score matches the given operator and value.
 *
 * Operators and expected value shapes:
 *   - eq: value must be a single string.
 *   - one_of: value must be a list of strings.
 *
 * Example:
 *     {"color": "green", "operator": "eq", "value": "pass"}
 *     {"color": "red", "operator": "one_of", "value": ["fail", "error"]}
 */
export type CategoricalColorConstraint = {
  /**
   * Allowed colors for metric threshold visualization in the UI.
   */
  color: MetricColor;
  operator: CategoricalColorConstraintOperator;
  value: string | Array<string>;
};

/** @internal */
export const CategoricalColorConstraintOperator$inboundSchema: z.ZodMiniType<
  CategoricalColorConstraintOperator,
  unknown
> = openEnums.inboundSchema(CategoricalColorConstraintOperator);

/** @internal */
export const CategoricalColorConstraintValue$inboundSchema: z.ZodMiniType<
  CategoricalColorConstraintValue,
  unknown
> = smartUnion([types.string(), z.array(types.string())]);

export function categoricalColorConstraintValueFromJSON(
  jsonString: string,
): SafeParseResult<CategoricalColorConstraintValue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CategoricalColorConstraintValue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CategoricalColorConstraintValue' from JSON`,
  );
}

/** @internal */
export const CategoricalColorConstraint$inboundSchema: z.ZodMiniType<
  CategoricalColorConstraint,
  unknown
> = z.object({
  color: MetricColor$inboundSchema,
  operator: CategoricalColorConstraintOperator$inboundSchema,
  value: smartUnion([types.string(), z.array(types.string())]),
});

export function categoricalColorConstraintFromJSON(
  jsonString: string,
): SafeParseResult<CategoricalColorConstraint, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CategoricalColorConstraint$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CategoricalColorConstraint' from JSON`,
  );
}
