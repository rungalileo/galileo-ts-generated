/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  CollaboratorRole,
  CollaboratorRole$inboundSchema,
} from "./collaboratorrole.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Permission, Permission$inboundSchema } from "./permission.js";

export type GroupCollaborator = {
  id: string;
  permissions?: Array<Permission> | undefined;
  role: CollaboratorRole;
  createdAt: Date;
  groupId: string;
  groupName: string;
};

/** @internal */
export const GroupCollaborator$inboundSchema: z.ZodMiniType<
  GroupCollaborator,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    permissions: types.optional(z.array(Permission$inboundSchema)),
    role: CollaboratorRole$inboundSchema,
    created_at: types.date(),
    group_id: types.string(),
    group_name: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "group_id": "groupId",
      "group_name": "groupName",
    });
  }),
);

export function groupCollaboratorFromJSON(
  jsonString: string,
): SafeParseResult<GroupCollaborator, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GroupCollaborator$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GroupCollaborator' from JSON`,
  );
}
