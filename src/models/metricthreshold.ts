/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Bucket = number | number;

/**
 * Threshold configuration for metrics.
 *
 * @remarks
 *
 * Defines how metric values are bucketed and displayed, including whether
 * lower or higher values are considered better.
 */
export type MetricThreshold = {
  /**
   * Whether the column should be inverted for thresholds, i.e. if True, lower is better.
   */
  inverted: boolean;
  /**
   * Threshold buckets for the column. If the column is a metric, these are the thresholds for the column.
   */
  buckets?: Array<number | number> | undefined;
  /**
   * Ordered list of strings that raw values get transformed to for displaying.
   */
  displayValueLevels?: Array<string> | undefined;
};

/** @internal */
export const Bucket$inboundSchema: z.ZodMiniType<Bucket, unknown> = smartUnion([
  types.number(),
  types.number(),
]);

export function bucketFromJSON(
  jsonString: string,
): SafeParseResult<Bucket, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Bucket$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Bucket' from JSON`,
  );
}

/** @internal */
export const MetricThreshold$inboundSchema: z.ZodMiniType<
  MetricThreshold,
  unknown
> = z.pipe(
  z.object({
    inverted: z._default(types.boolean(), false),
    buckets: types.optional(
      z.array(smartUnion([types.number(), types.number()])),
    ),
    display_value_levels: types.optional(z.array(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "display_value_levels": "displayValueLevels",
    });
  }),
);

export function metricThresholdFromJSON(
  jsonString: string,
): SafeParseResult<MetricThreshold, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MetricThreshold$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MetricThreshold' from JSON`,
  );
}
