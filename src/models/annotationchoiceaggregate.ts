/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: e871e36c7738
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AnnotationChoiceAggregate = {
  annotationType: "choice";
  counts: { [k: string]: number };
  unratedCount: number;
};

/** @internal */
export const AnnotationChoiceAggregate$inboundSchema: z.ZodMiniType<
  AnnotationChoiceAggregate,
  unknown
> = z.pipe(
  z.object({
    annotation_type: types.literal("choice"),
    counts: z.record(z.string(), types.number()),
    unrated_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
      "unrated_count": "unratedCount",
    });
  }),
);

export function annotationChoiceAggregateFromJSON(
  jsonString: string,
): SafeParseResult<AnnotationChoiceAggregate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnotationChoiceAggregate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnotationChoiceAggregate' from JSON`,
  );
}
