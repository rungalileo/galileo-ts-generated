/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f4a8add570e3
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  RuleOperator,
  RuleOperator$inboundSchema,
  RuleOperator$outboundSchema,
} from "./ruleoperator.js";

/**
 * Value to compare with for this metric (right hand side).
 */
export type RuleTargetValue = string | number | number | Array<any>;

export type Rule = {
  /**
   * Name of the metric.
   */
  metric: string;
  operator: RuleOperator;
  /**
   * Value to compare with for this metric (right hand side).
   */
  targetValue: string | number | number | Array<any> | null;
};

/** @internal */
export const RuleTargetValue$inboundSchema: z.ZodMiniType<
  RuleTargetValue,
  unknown
> = smartUnion([
  types.string(),
  types.number(),
  types.number(),
  z.array(z.any()),
]);
/** @internal */
export type RuleTargetValue$Outbound = string | number | number | Array<any>;

/** @internal */
export const RuleTargetValue$outboundSchema: z.ZodMiniType<
  RuleTargetValue$Outbound,
  RuleTargetValue
> = smartUnion([z.string(), z.number(), z.int(), z.array(z.any())]);

export function ruleTargetValueToJSON(
  ruleTargetValue: RuleTargetValue,
): string {
  return JSON.stringify(RuleTargetValue$outboundSchema.parse(ruleTargetValue));
}
export function ruleTargetValueFromJSON(
  jsonString: string,
): SafeParseResult<RuleTargetValue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RuleTargetValue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RuleTargetValue' from JSON`,
  );
}

/** @internal */
export const Rule$inboundSchema: z.ZodMiniType<Rule, unknown> = z.pipe(
  z.object({
    metric: types.string(),
    operator: RuleOperator$inboundSchema,
    target_value: types.nullable(
      smartUnion([
        types.string(),
        types.number(),
        types.number(),
        z.array(z.any()),
      ]),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "target_value": "targetValue",
    });
  }),
);
/** @internal */
export type Rule$Outbound = {
  metric: string;
  operator: string;
  target_value: string | number | number | Array<any> | null;
};

/** @internal */
export const Rule$outboundSchema: z.ZodMiniType<Rule$Outbound, Rule> = z.pipe(
  z.object({
    metric: z.string(),
    operator: RuleOperator$outboundSchema,
    targetValue: z.nullable(
      smartUnion([z.string(), z.number(), z.int(), z.array(z.any())]),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      targetValue: "target_value",
    });
  }),
);

export function ruleToJSON(rule: Rule): string {
  return JSON.stringify(Rule$outboundSchema.parse(rule));
}
export function ruleFromJSON(
  jsonString: string,
): SafeParseResult<Rule, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Rule$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Rule' from JSON`,
  );
}
