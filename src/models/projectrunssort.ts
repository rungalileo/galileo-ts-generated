/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: c0ad4919bac6
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type ProjectRunsSort = {
  name: "runs";
  ascending?: boolean | undefined;
  sortType?: "custom" | undefined;
};

/** @internal */
export type ProjectRunsSort$Outbound = {
  name: "runs";
  ascending: boolean;
  sort_type: "custom";
};

/** @internal */
export const ProjectRunsSort$outboundSchema: z.ZodMiniType<
  ProjectRunsSort$Outbound,
  ProjectRunsSort
> = z.pipe(
  z.object({
    name: z.literal("runs"),
    ascending: z._default(z.boolean(), true),
    sortType: z._default(z.literal("custom"), "custom" as const),
  }),
  z.transform((v) => {
    return remap$(v, {
      sortType: "sort_type",
    });
  }),
);

export function projectRunsSortToJSON(
  projectRunsSort: ProjectRunsSort,
): string {
  return JSON.stringify(ProjectRunsSort$outboundSchema.parse(projectRunsSort));
}
