/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ScorerResponse,
  ScorerResponse$inboundSchema,
} from "./scorerresponse.js";

export type ListScorersResponse = {
  startingToken: number;
  limit: number;
  paginated: boolean;
  nextStartingToken?: number | null | undefined;
  scorers?: Array<ScorerResponse> | undefined;
};

/** @internal */
export const ListScorersResponse$inboundSchema: z.ZodMiniType<
  ListScorersResponse,
  unknown
> = z.pipe(
  z.object({
    starting_token: z._default(types.number(), 0),
    limit: z._default(types.number(), 100),
    paginated: z._default(types.boolean(), false),
    next_starting_token: z.optional(z.nullable(types.number())),
    scorers: types.optional(z.array(ScorerResponse$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "starting_token": "startingToken",
      "next_starting_token": "nextStartingToken",
    });
  }),
);

export function listScorersResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListScorersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListScorersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListScorersResponse' from JSON`,
  );
}
