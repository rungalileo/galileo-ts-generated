/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AnnotationTagsAggregate = {
  annotationType: "tags";
  counts: { [k: string]: number };
  unratedCount: number;
};

/** @internal */
export const AnnotationTagsAggregate$inboundSchema: z.ZodMiniType<
  AnnotationTagsAggregate,
  unknown
> = z.pipe(
  z.object({
    annotation_type: types.literal("tags"),
    counts: z.record(z.string(), types.number()),
    unrated_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
      "unrated_count": "unratedCount",
    });
  }),
);

export function annotationTagsAggregateFromJSON(
  jsonString: string,
): SafeParseResult<AnnotationTagsAggregate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnotationTagsAggregate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnotationTagsAggregate' from JSON`,
  );
}
