/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f4fafb7e56bb
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { ActionResult, ActionResult$inboundSchema } from "./actionresult.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ExecutionStatus,
  ExecutionStatus$inboundSchema,
} from "./executionstatus.js";
import {
  MetricComputation,
  MetricComputation$inboundSchema,
} from "./metriccomputation.js";
import { RulesetResult, RulesetResult$inboundSchema } from "./rulesetresult.js";
import { StageMetadata, StageMetadata$inboundSchema } from "./stagemetadata.js";
import { TraceMetadata, TraceMetadata$inboundSchema } from "./tracemetadata.js";

export type InvokeResponse = {
  /**
   * Status of the execution.
   */
  status?: ExecutionStatus | undefined;
  apiVersion: string;
  /**
   * Text from the request after processing the rules.
   */
  text: string;
  traceMetadata: TraceMetadata;
  stageMetadata: StageMetadata;
  /**
   * Results of the rule execution.
   */
  rulesetResults?: Array<RulesetResult> | undefined;
  /**
   * Results of the metric computation.
   */
  metricResults?: { [k: string]: MetricComputation } | undefined;
  actionResult: ActionResult;
  /**
   * Optional additional metadata. This being echoed back from the request.
   */
  metadata?: { [k: string]: string } | null | undefined;
  /**
   * Optional additional HTTP headers that should be included in the response.
   */
  headers?: { [k: string]: string } | null | undefined;
};

/** @internal */
export const InvokeResponse$inboundSchema: z.ZodMiniType<
  InvokeResponse,
  unknown
> = z.pipe(
  z.object({
    status: types.optional(ExecutionStatus$inboundSchema),
    api_version: z._default(types.string(), "1.0.0"),
    text: types.string(),
    trace_metadata: TraceMetadata$inboundSchema,
    stage_metadata: StageMetadata$inboundSchema,
    ruleset_results: types.optional(z.array(RulesetResult$inboundSchema)),
    metric_results: types.optional(
      z.record(z.string(), MetricComputation$inboundSchema),
    ),
    action_result: ActionResult$inboundSchema,
    metadata: z.optional(z.nullable(z.record(z.string(), types.string()))),
    headers: z.optional(z.nullable(z.record(z.string(), types.string()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "api_version": "apiVersion",
      "trace_metadata": "traceMetadata",
      "stage_metadata": "stageMetadata",
      "ruleset_results": "rulesetResults",
      "metric_results": "metricResults",
      "action_result": "actionResult",
    });
  }),
);

export function invokeResponseFromJSON(
  jsonString: string,
): SafeParseResult<InvokeResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InvokeResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InvokeResponse' from JSON`,
  );
}
