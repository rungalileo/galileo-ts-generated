/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f96d3c24869c
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  AnnotationQueueResponse,
  AnnotationQueueResponse$inboundSchema,
} from "./annotationqueueresponse.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ListAnnotationQueueResponse = {
  startingToken: number;
  limit: number;
  paginated: boolean;
  nextStartingToken?: number | null | undefined;
  annotationQueues: Array<AnnotationQueueResponse>;
};

/** @internal */
export const ListAnnotationQueueResponse$inboundSchema: z.ZodMiniType<
  ListAnnotationQueueResponse,
  unknown
> = z.pipe(
  z.object({
    starting_token: z._default(types.number(), 0),
    limit: z._default(types.number(), 100),
    paginated: z._default(types.boolean(), false),
    next_starting_token: z.optional(z.nullable(types.number())),
    annotation_queues: z.array(AnnotationQueueResponse$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "starting_token": "startingToken",
      "next_starting_token": "nextStartingToken",
      "annotation_queues": "annotationQueues",
    });
  }),
);

export function listAnnotationQueueResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListAnnotationQueueResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAnnotationQueueResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAnnotationQueueResponse' from JSON`,
  );
}
