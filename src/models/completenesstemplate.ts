/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 81015491c06b
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  FewShotExample,
  FewShotExample$inboundSchema,
  FewShotExample$Outbound,
  FewShotExample$outboundSchema,
} from "./fewshotexample.js";

export type CompletenessTemplate = {
  /**
   * System prompt for the metric.
   */
  metricSystemPrompt?: string | null | undefined;
  /**
   * Description of what the metric should do.
   */
  metricDescription?: string | null | undefined;
  valueFieldName?: string | undefined;
  /**
   * Field name to look for in the chainpoll response, for the explanation.
   */
  explanationFieldName?: string | undefined;
  template?: string | undefined;
  /**
   * Few-shot examples for the metric.
   */
  metricFewShotExamples?: Array<FewShotExample> | undefined;
  /**
   * Response schema for the output
   */
  responseSchema?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const CompletenessTemplate$inboundSchema: z.ZodMiniType<
  CompletenessTemplate,
  unknown
> = z.pipe(
  z.object({
    metric_system_prompt: z.optional(z.nullable(types.string())),
    metric_description: z.optional(z.nullable(types.string())),
    value_field_name: z._default(types.string(), "completeness"),
    explanation_field_name: z._default(types.string(), "explanation"),
    template: z._default(
      types.string(),
      "I asked someone to answer a question based on one or more documents. On a scale of 0 to 1, tell me how well their response covered the relevant information from the documents.\n\nHere is what I said to them, as a JSON string:\n\n```\n{query_json}\n```\n\nHere is what they told me, as a JSON string:\n\n```\n{response_json}\n```\n\nRespond in the following JSON format:\n\n```\n{{\n    \"explanation\": string,\n    \"completeness\": number\n}}\n```\n\n\"explanation\": A string with your step-by-step reasoning process. List out each piece of information covered in the documents. For each one, explain why it was or was not relevant to the question, and how well the response covered it. Do *not* give an overall assessment of the response here, just think step by step about each piece of information, one at a time. Present your work in a document-by-document format, considering each document separately, ensure the value is a valid string.\n\n\"completeness\": A floating-point number rating the Completeness of the response on a scale of 0 to 1. This number should equal the amount of relevant information that was comprehensively covered in the response, divided by the total amount of relevant information in the documents.\n\nYou must respond with a valid JSON string.",
    ),
    metric_few_shot_examples: types.optional(
      z.array(FewShotExample$inboundSchema),
    ),
    response_schema: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metric_system_prompt": "metricSystemPrompt",
      "metric_description": "metricDescription",
      "value_field_name": "valueFieldName",
      "explanation_field_name": "explanationFieldName",
      "metric_few_shot_examples": "metricFewShotExamples",
      "response_schema": "responseSchema",
    });
  }),
);
/** @internal */
export type CompletenessTemplate$Outbound = {
  metric_system_prompt?: string | null | undefined;
  metric_description?: string | null | undefined;
  value_field_name: string;
  explanation_field_name: string;
  template: string;
  metric_few_shot_examples?: Array<FewShotExample$Outbound> | undefined;
  response_schema?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const CompletenessTemplate$outboundSchema: z.ZodMiniType<
  CompletenessTemplate$Outbound,
  CompletenessTemplate
> = z.pipe(
  z.object({
    metricSystemPrompt: z.optional(z.nullable(z.string())),
    metricDescription: z.optional(z.nullable(z.string())),
    valueFieldName: z._default(z.string(), "completeness"),
    explanationFieldName: z._default(z.string(), "explanation"),
    template: z._default(
      z.string(),
      "I asked someone to answer a question based on one or more documents. On a scale of 0 to 1, tell me how well their response covered the relevant information from the documents.\n\nHere is what I said to them, as a JSON string:\n\n```\n{query_json}\n```\n\nHere is what they told me, as a JSON string:\n\n```\n{response_json}\n```\n\nRespond in the following JSON format:\n\n```\n{{\n    \"explanation\": string,\n    \"completeness\": number\n}}\n```\n\n\"explanation\": A string with your step-by-step reasoning process. List out each piece of information covered in the documents. For each one, explain why it was or was not relevant to the question, and how well the response covered it. Do *not* give an overall assessment of the response here, just think step by step about each piece of information, one at a time. Present your work in a document-by-document format, considering each document separately, ensure the value is a valid string.\n\n\"completeness\": A floating-point number rating the Completeness of the response on a scale of 0 to 1. This number should equal the amount of relevant information that was comprehensively covered in the response, divided by the total amount of relevant information in the documents.\n\nYou must respond with a valid JSON string.",
    ),
    metricFewShotExamples: z.optional(z.array(FewShotExample$outboundSchema)),
    responseSchema: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      metricSystemPrompt: "metric_system_prompt",
      metricDescription: "metric_description",
      valueFieldName: "value_field_name",
      explanationFieldName: "explanation_field_name",
      metricFewShotExamples: "metric_few_shot_examples",
      responseSchema: "response_schema",
    });
  }),
);

export function completenessTemplateToJSON(
  completenessTemplate: CompletenessTemplate,
): string {
  return JSON.stringify(
    CompletenessTemplate$outboundSchema.parse(completenessTemplate),
  );
}
export function completenessTemplateFromJSON(
  jsonString: string,
): SafeParseResult<CompletenessTemplate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompletenessTemplate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompletenessTemplate' from JSON`,
  );
}
