/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a4b4ec72e426
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type CategoricalMetricInfo = {
  /**
   * Discriminator: categorical metrics aggregated as per-label counts
   */
  aggregationType: "categorical";
  /**
   * Unique identifier for the metric
   */
  name: string;
  /**
   * Human-readable display name for the metric
   */
  label: string;
  /**
   * Count of occurrences per category label across records
   */
  categoryCounts?: { [k: string]: number } | undefined;
};

/** @internal */
export const CategoricalMetricInfo$inboundSchema: z.ZodMiniType<
  CategoricalMetricInfo,
  unknown
> = z.pipe(
  z.object({
    aggregation_type: types.literal("categorical"),
    name: types.string(),
    label: types.string(),
    category_counts: types.optional(z.record(z.string(), types.number())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "aggregation_type": "aggregationType",
      "category_counts": "categoryCounts",
    });
  }),
);

export function categoricalMetricInfoFromJSON(
  jsonString: string,
): SafeParseResult<CategoricalMetricInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CategoricalMetricInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CategoricalMetricInfo' from JSON`,
  );
}
