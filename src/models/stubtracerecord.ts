/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 57e6bdd96b0f
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as discriminatedUnionTypes from "../types/discriminatedUnion.js";
import { discriminatedUnion } from "../types/discriminatedUnion.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ExtendedAgentSpanRecordWithChildren,
  ExtendedAgentSpanRecordWithChildren$inboundSchema,
} from "./extendedagentspanrecordwithchildren.js";
import {
  ExtendedControlSpanRecord,
  ExtendedControlSpanRecord$inboundSchema,
} from "./extendedcontrolspanrecord.js";
import {
  ExtendedHttpSpanRecordWithChildren,
  ExtendedHttpSpanRecordWithChildren$inboundSchema,
} from "./extendedhttpspanrecordwithchildren.js";
import {
  ExtendedLlmSpanRecord,
  ExtendedLlmSpanRecord$inboundSchema,
} from "./extendedllmspanrecord.js";
import {
  ExtendedRetrieverSpanRecordWithChildren,
  ExtendedRetrieverSpanRecordWithChildren$inboundSchema,
} from "./extendedretrieverspanrecordwithchildren.js";
import {
  ExtendedToolSpanRecordWithChildren,
  ExtendedToolSpanRecordWithChildren$inboundSchema,
} from "./extendedtoolspanrecordwithchildren.js";
import {
  ExtendedWorkflowSpanRecordWithChildren,
  ExtendedWorkflowSpanRecordWithChildren$inboundSchema,
} from "./extendedworkflowspanrecordwithchildren.js";

export type StubTraceRecordSpan =
  | ExtendedAgentSpanRecordWithChildren
  | ExtendedControlSpanRecord
  | ExtendedHttpSpanRecordWithChildren
  | ExtendedLlmSpanRecord
  | ExtendedRetrieverSpanRecordWithChildren
  | ExtendedToolSpanRecordWithChildren
  | ExtendedWorkflowSpanRecordWithChildren
  | discriminatedUnionTypes.Unknown<"type">;

/**
 * Placeholder for a trace referenced by spans but not yet ingested.
 *
 * @remarks
 *
 * Synthesized when one or more spans declare trace_id=X but no
 * TraceRecord with that id exists in storage. Holds the orphan spans
 * together so the client can render them under a single root.
 *
 * Extends ExtendedRecordWithChildSpans so isinstance checks work
 * uniformly for both real and stub traces.
 */
export type StubTraceRecord = {
  spans?:
    | Array<
      | ExtendedAgentSpanRecordWithChildren
      | ExtendedControlSpanRecord
      | ExtendedHttpSpanRecordWithChildren
      | ExtendedLlmSpanRecord
      | ExtendedRetrieverSpanRecordWithChildren
      | ExtendedToolSpanRecordWithChildren
      | ExtendedWorkflowSpanRecordWithChildren
      | discriminatedUnionTypes.Unknown<"type">
    >
    | undefined;
  /**
   * Discriminator; identifies this as a synthesized placeholder, not a real trace.
   */
  type: "stub_trace";
  /**
   * ID of the missing trace, taken from span trace_id references.
   */
  id: string;
  /**
   * Project ID inferred from child spans, if all agree; otherwise None.
   */
  projectId?: string | null | undefined;
  /**
   * Run ID inferred from child spans, if all agree; otherwise None.
   */
  runId?: string | null | undefined;
  /**
   * Session ID inferred from child spans, if all agree; otherwise None.
   */
  sessionId?: string | null | undefined;
  /**
   * Earliest created_at among child spans; used to order under a session.
   */
  createdAt: Date;
};

/** @internal */
export const StubTraceRecordSpan$inboundSchema: z.ZodMiniType<
  StubTraceRecordSpan,
  unknown
> = discriminatedUnion("type", {
  agent: ExtendedAgentSpanRecordWithChildren$inboundSchema,
  control: ExtendedControlSpanRecord$inboundSchema,
  http: ExtendedHttpSpanRecordWithChildren$inboundSchema,
  llm: ExtendedLlmSpanRecord$inboundSchema,
  retriever: ExtendedRetrieverSpanRecordWithChildren$inboundSchema,
  tool: ExtendedToolSpanRecordWithChildren$inboundSchema,
  workflow: ExtendedWorkflowSpanRecordWithChildren$inboundSchema,
});

export function stubTraceRecordSpanFromJSON(
  jsonString: string,
): SafeParseResult<StubTraceRecordSpan, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StubTraceRecordSpan$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StubTraceRecordSpan' from JSON`,
  );
}

/** @internal */
export const StubTraceRecord$inboundSchema: z.ZodMiniType<
  StubTraceRecord,
  unknown
> = z.pipe(
  z.object({
    spans: types.optional(z.array(discriminatedUnion("type", {
      agent: ExtendedAgentSpanRecordWithChildren$inboundSchema,
      control: ExtendedControlSpanRecord$inboundSchema,
      http: ExtendedHttpSpanRecordWithChildren$inboundSchema,
      llm: ExtendedLlmSpanRecord$inboundSchema,
      retriever: ExtendedRetrieverSpanRecordWithChildren$inboundSchema,
      tool: ExtendedToolSpanRecordWithChildren$inboundSchema,
      workflow: ExtendedWorkflowSpanRecordWithChildren$inboundSchema,
    }))),
    type: types.literal("stub_trace"),
    id: types.string(),
    project_id: z.optional(z.nullable(types.string())),
    run_id: z.optional(z.nullable(types.string())),
    session_id: z.optional(z.nullable(types.string())),
    created_at: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "project_id": "projectId",
      "run_id": "runId",
      "session_id": "sessionId",
      "created_at": "createdAt",
    });
  }),
);

export function stubTraceRecordFromJSON(
  jsonString: string,
): SafeParseResult<StubTraceRecord, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StubTraceRecord$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StubTraceRecord' from JSON`,
  );
}
