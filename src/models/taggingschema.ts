/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 80ffe94c845a
 */

import * as z from "zod/v4-mini";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";

/**
 * Supported NER Tagging schemas.
 *
 * @remarks
 *
 * A tagging schema is a definition of the way NER data is formatted. The schema
 * letters define the tags used within it.
 *     ex:
 *         BIOES - B means before (a token), I means in (a token),
 *             E means end (of a token), S means single (length token)
 * See https://en.wikipedia.org/wiki/Inside%E2%80%93outside%E2%80%93beginning_(tagging)
 */
export const TaggingSchema = {
  Bio: "BIO",
  Bioes: "BIOES",
  Bilou: "BILOU",
} as const;
/**
 * Supported NER Tagging schemas.
 *
 * @remarks
 *
 * A tagging schema is a definition of the way NER data is formatted. The schema
 * letters define the tags used within it.
 *     ex:
 *         BIOES - B means before (a token), I means in (a token),
 *             E means end (of a token), S means single (length token)
 * See https://en.wikipedia.org/wiki/Inside%E2%80%93outside%E2%80%93beginning_(tagging)
 */
export type TaggingSchema = OpenEnum<typeof TaggingSchema>;

/** @internal */
export const TaggingSchema$inboundSchema: z.ZodMiniType<
  TaggingSchema,
  unknown
> = openEnums.inboundSchema(TaggingSchema);
/** @internal */
export const TaggingSchema$outboundSchema: z.ZodMiniType<
  string,
  TaggingSchema
> = openEnums.outboundSchema(TaggingSchema);
