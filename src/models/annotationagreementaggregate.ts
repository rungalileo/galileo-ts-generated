/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 418d1b1f8221
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  AnnotationAgreementBucket,
  AnnotationAgreementBucket$inboundSchema,
} from "./annotationagreementbucket.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AnnotationAgreementAggregate = {
  buckets: Array<AnnotationAgreementBucket>;
  averageAgreement: number;
};

/** @internal */
export const AnnotationAgreementAggregate$inboundSchema: z.ZodMiniType<
  AnnotationAgreementAggregate,
  unknown
> = z.pipe(
  z.object({
    buckets: z.array(AnnotationAgreementBucket$inboundSchema),
    average_agreement: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "average_agreement": "averageAgreement",
    });
  }),
);

export function annotationAgreementAggregateFromJSON(
  jsonString: string,
): SafeParseResult<AnnotationAgreementAggregate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnotationAgreementAggregate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnotationAgreementAggregate' from JSON`,
  );
}
