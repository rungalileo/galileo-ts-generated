/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 9801d0fd4142
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  CollaboratorRole,
  CollaboratorRole$inboundSchema,
} from "./collaboratorrole.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Permission, Permission$inboundSchema } from "./permission.js";

export type UserCollaborator = {
  id: string;
  permissions?: Array<Permission> | undefined;
  role: CollaboratorRole;
  createdAt: Date;
  userId: string;
  firstName: string | null;
  lastName: string | null;
  email: string;
};

/** @internal */
export const UserCollaborator$inboundSchema: z.ZodMiniType<
  UserCollaborator,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    permissions: types.optional(z.array(Permission$inboundSchema)),
    role: CollaboratorRole$inboundSchema,
    created_at: types.date(),
    user_id: types.string(),
    first_name: types.nullable(types.string()),
    last_name: types.nullable(types.string()),
    email: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "user_id": "userId",
      "first_name": "firstName",
      "last_name": "lastName",
    });
  }),
);

export function userCollaboratorFromJSON(
  jsonString: string,
): SafeParseResult<UserCollaborator, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UserCollaborator$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UserCollaborator' from JSON`,
  );
}
