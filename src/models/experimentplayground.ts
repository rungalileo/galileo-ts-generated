/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ExperimentPlayground = {
  playgroundId?: string | null | undefined;
  name?: string | null | undefined;
};

/** @internal */
export const ExperimentPlayground$inboundSchema: z.ZodMiniType<
  ExperimentPlayground,
  unknown
> = z.pipe(
  z.object({
    playground_id: z.optional(z.nullable(types.string())),
    name: z.optional(z.nullable(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "playground_id": "playgroundId",
    });
  }),
);

export function experimentPlaygroundFromJSON(
  jsonString: string,
): SafeParseResult<ExperimentPlayground, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExperimentPlayground$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExperimentPlayground' from JSON`,
  );
}
