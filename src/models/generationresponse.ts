/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type GenerationResponse = {
  taskResultId: string;
};

/** @internal */
export const GenerationResponse$inboundSchema: z.ZodMiniType<
  GenerationResponse,
  unknown
> = z.pipe(
  z.object({
    task_result_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "task_result_id": "taskResultId",
    });
  }),
);

export function generationResponseFromJSON(
  jsonString: string,
): SafeParseResult<GenerationResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GenerationResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GenerationResponse' from JSON`,
  );
}
