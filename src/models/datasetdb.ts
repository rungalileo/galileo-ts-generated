/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6c576db3f0e1
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Permission, Permission$inboundSchema } from "./permission.js";
import { UserInfo, UserInfo$inboundSchema } from "./userinfo.js";

export type DatasetDB = {
  id: string;
  permissions?: Array<Permission> | undefined;
  name: string;
  createdAt: Date;
  updatedAt: Date;
  projectCount: number;
  numRows: number | null;
  columnNames: Array<string> | null;
  createdByUser: UserInfo | null;
  currentVersionIndex: number;
  draft: boolean;
};

/** @internal */
export const DatasetDB$inboundSchema: z.ZodMiniType<DatasetDB, unknown> = z
  .pipe(
    z.object({
      id: types.string(),
      permissions: types.optional(z.array(Permission$inboundSchema)),
      name: types.string(),
      created_at: types.date(),
      updated_at: types.date(),
      project_count: types.number(),
      num_rows: types.nullable(types.number()),
      column_names: types.nullable(z.array(types.string())),
      created_by_user: types.nullable(UserInfo$inboundSchema),
      current_version_index: types.number(),
      draft: types.boolean(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "updated_at": "updatedAt",
        "project_count": "projectCount",
        "num_rows": "numRows",
        "column_names": "columnNames",
        "created_by_user": "createdByUser",
        "current_version_index": "currentVersionIndex",
      });
    }),
  );

export function datasetDBFromJSON(
  jsonString: string,
): SafeParseResult<DatasetDB, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DatasetDB$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DatasetDB' from JSON`,
  );
}
