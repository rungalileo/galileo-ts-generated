/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { smartUnion } from "../types/smartUnion.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  ExecutionStatus,
  ExecutionStatus$inboundSchema,
} from "./executionstatus.js";
import { RuleOperator, RuleOperator$inboundSchema } from "./ruleoperator.js";

/**
 * Value to compare with for this metric (right hand side).
 */
export type RuleResultTargetValue = string | number | number | Array<any>;

export type RuleResult = {
  /**
   * Status of the execution.
   */
  status?: ExecutionStatus | undefined;
  /**
   * Name of the metric.
   */
  metric: string;
  operator: RuleOperator;
  /**
   * Value to compare with for this metric (right hand side).
   */
  targetValue: string | number | number | Array<any> | null;
  /**
   * Result of the metric computation.
   */
  value?: any | null | undefined;
  /**
   * Execution time for the rule in seconds.
   */
  executionTime?: number | null | undefined;
};

/** @internal */
export const RuleResultTargetValue$inboundSchema: z.ZodMiniType<
  RuleResultTargetValue,
  unknown
> = smartUnion([
  types.string(),
  types.number(),
  types.number(),
  z.array(z.any()),
]);

export function ruleResultTargetValueFromJSON(
  jsonString: string,
): SafeParseResult<RuleResultTargetValue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RuleResultTargetValue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RuleResultTargetValue' from JSON`,
  );
}

/** @internal */
export const RuleResult$inboundSchema: z.ZodMiniType<RuleResult, unknown> = z
  .pipe(
    z.object({
      status: types.optional(ExecutionStatus$inboundSchema),
      metric: types.string(),
      operator: RuleOperator$inboundSchema,
      target_value: types.nullable(
        smartUnion([
          types.string(),
          types.number(),
          types.number(),
          z.array(z.any()),
        ]),
      ),
      value: z.optional(z.nullable(z.any())),
      execution_time: z.optional(z.nullable(types.number())),
    }),
    z.transform((v) => {
      return remap$(v, {
        "target_value": "targetValue",
        "execution_time": "executionTime",
      });
    }),
  );

export function ruleResultFromJSON(
  jsonString: string,
): SafeParseResult<RuleResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RuleResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RuleResult' from JSON`,
  );
}
