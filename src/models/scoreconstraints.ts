/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 38ab99e32be8
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ScoreConstraints = {
  annotationType: "score";
  min: number;
  max: number;
};

/** @internal */
export const ScoreConstraints$inboundSchema: z.ZodMiniType<
  ScoreConstraints,
  unknown
> = z.pipe(
  z.object({
    annotation_type: types.literal("score"),
    min: types.number(),
    max: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
    });
  }),
);
/** @internal */
export type ScoreConstraints$Outbound = {
  annotation_type: "score";
  min: number;
  max: number;
};

/** @internal */
export const ScoreConstraints$outboundSchema: z.ZodMiniType<
  ScoreConstraints$Outbound,
  ScoreConstraints
> = z.pipe(
  z.object({
    annotationType: z.literal("score"),
    min: z.int(),
    max: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      annotationType: "annotation_type",
    });
  }),
);

export function scoreConstraintsToJSON(
  scoreConstraints: ScoreConstraints,
): string {
  return JSON.stringify(
    ScoreConstraints$outboundSchema.parse(scoreConstraints),
  );
}
export function scoreConstraintsFromJSON(
  jsonString: string,
): SafeParseResult<ScoreConstraints, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScoreConstraints$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScoreConstraints' from JSON`,
  );
}
