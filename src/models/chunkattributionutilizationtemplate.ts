/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6c5f46f4b78b
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  FewShotExample,
  FewShotExample$inboundSchema,
  FewShotExample$Outbound,
  FewShotExample$outboundSchema,
} from "./fewshotexample.js";

export type ChunkAttributionUtilizationTemplate = {
  /**
   * System prompt for the metric.
   */
  metricSystemPrompt?: string | null | undefined;
  /**
   * Description of what the metric should do.
   */
  metricDescription?: string | null | undefined;
  /**
   * Field name to look for in the chainpoll response, for the rating.
   */
  valueFieldName?: string | undefined;
  /**
   * Field name to look for in the chainpoll response, for the explanation.
   */
  explanationFieldName?: string | undefined;
  template?: string | undefined;
  /**
   * Few-shot examples for the metric.
   */
  metricFewShotExamples?: Array<FewShotExample> | undefined;
  /**
   * Response schema for the output
   */
  responseSchema?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const ChunkAttributionUtilizationTemplate$inboundSchema: z.ZodMiniType<
  ChunkAttributionUtilizationTemplate,
  unknown
> = z.pipe(
  z.object({
    metric_system_prompt: z.optional(z.nullable(types.string())),
    metric_description: z.optional(z.nullable(types.string())),
    value_field_name: z._default(types.string(), "rating"),
    explanation_field_name: z._default(types.string(), "explanation"),
    template: z._default(
      types.string(),
      "I asked someone to answer a question based on one or more documents. You will tell me which of the documents their answer was sourced from, and which specific sentences from the documents they used.\n\nHere are the documents, with each document split up into sentences. Each sentence is given a unique key, such as '0a' for the first sentence of Document 0. You'll use these keys in your response to identify which sentences were used.\n\n```\n{chunks}\n```\n\nThe question was:\n\n```\n{question}\n```\n\nTheir response was:\n\n```\n{response}\n```\n\nRespond with a JSON object matching this schema:\n\n```\n{{\n  \"source_sentence_keys\": [string]\n}}\n```\n\nThe source_sentence_keys field is a list identifying the sentences in the documents that were used to construct the answer. Each entry MUST be a sentence key, such as '0a', that appears in the document list above. Include the key of every sentence that was used to construct the answer, even if it was not used in its entirety. Omit keys for sentences that were not used, and could have been removed from the document without affecting the answer.\n\nYou must respond with a valid JSON string.",
    ),
    metric_few_shot_examples: types.optional(
      z.array(FewShotExample$inboundSchema),
    ),
    response_schema: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metric_system_prompt": "metricSystemPrompt",
      "metric_description": "metricDescription",
      "value_field_name": "valueFieldName",
      "explanation_field_name": "explanationFieldName",
      "metric_few_shot_examples": "metricFewShotExamples",
      "response_schema": "responseSchema",
    });
  }),
);
/** @internal */
export type ChunkAttributionUtilizationTemplate$Outbound = {
  metric_system_prompt?: string | null | undefined;
  metric_description?: string | null | undefined;
  value_field_name: string;
  explanation_field_name: string;
  template: string;
  metric_few_shot_examples?: Array<FewShotExample$Outbound> | undefined;
  response_schema?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const ChunkAttributionUtilizationTemplate$outboundSchema: z.ZodMiniType<
  ChunkAttributionUtilizationTemplate$Outbound,
  ChunkAttributionUtilizationTemplate
> = z.pipe(
  z.object({
    metricSystemPrompt: z.optional(z.nullable(z.string())),
    metricDescription: z.optional(z.nullable(z.string())),
    valueFieldName: z._default(z.string(), "rating"),
    explanationFieldName: z._default(z.string(), "explanation"),
    template: z._default(
      z.string(),
      "I asked someone to answer a question based on one or more documents. You will tell me which of the documents their answer was sourced from, and which specific sentences from the documents they used.\n\nHere are the documents, with each document split up into sentences. Each sentence is given a unique key, such as '0a' for the first sentence of Document 0. You'll use these keys in your response to identify which sentences were used.\n\n```\n{chunks}\n```\n\nThe question was:\n\n```\n{question}\n```\n\nTheir response was:\n\n```\n{response}\n```\n\nRespond with a JSON object matching this schema:\n\n```\n{{\n  \"source_sentence_keys\": [string]\n}}\n```\n\nThe source_sentence_keys field is a list identifying the sentences in the documents that were used to construct the answer. Each entry MUST be a sentence key, such as '0a', that appears in the document list above. Include the key of every sentence that was used to construct the answer, even if it was not used in its entirety. Omit keys for sentences that were not used, and could have been removed from the document without affecting the answer.\n\nYou must respond with a valid JSON string.",
    ),
    metricFewShotExamples: z.optional(z.array(FewShotExample$outboundSchema)),
    responseSchema: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      metricSystemPrompt: "metric_system_prompt",
      metricDescription: "metric_description",
      valueFieldName: "value_field_name",
      explanationFieldName: "explanation_field_name",
      metricFewShotExamples: "metric_few_shot_examples",
      responseSchema: "response_schema",
    });
  }),
);

export function chunkAttributionUtilizationTemplateToJSON(
  chunkAttributionUtilizationTemplate: ChunkAttributionUtilizationTemplate,
): string {
  return JSON.stringify(
    ChunkAttributionUtilizationTemplate$outboundSchema.parse(
      chunkAttributionUtilizationTemplate,
    ),
  );
}
export function chunkAttributionUtilizationTemplateFromJSON(
  jsonString: string,
): SafeParseResult<ChunkAttributionUtilizationTemplate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      ChunkAttributionUtilizationTemplate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ChunkAttributionUtilizationTemplate' from JSON`,
  );
}
