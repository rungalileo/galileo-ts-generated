/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Response model for validating a scorer based on log records.
 *
 * @remarks
 *
 * Returns the uuid of the experiment created with the copied log records to store the metric testing results.
 */
export type ValidateScorerLogRecordResponse = {
  metricsExperimentId: string;
};

/** @internal */
export const ValidateScorerLogRecordResponse$inboundSchema: z.ZodMiniType<
  ValidateScorerLogRecordResponse,
  unknown
> = z.pipe(
  z.object({
    metrics_experiment_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "metrics_experiment_id": "metricsExperimentId",
    });
  }),
);

export function validateScorerLogRecordResponseFromJSON(
  jsonString: string,
): SafeParseResult<ValidateScorerLogRecordResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ValidateScorerLogRecordResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ValidateScorerLogRecordResponse' from JSON`,
  );
}
