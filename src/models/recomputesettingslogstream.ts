/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f76aee9be8a8
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type RecomputeSettingsLogStream = {
  mode: "log_stream_filters";
  runId: string;
  filters: Array<any>;
};

/** @internal */
export const RecomputeSettingsLogStream$inboundSchema: z.ZodMiniType<
  RecomputeSettingsLogStream,
  unknown
> = z.pipe(
  z.object({
    mode: types.literal("log_stream_filters"),
    run_id: types.string(),
    filters: z.array(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "run_id": "runId",
    });
  }),
);
/** @internal */
export type RecomputeSettingsLogStream$Outbound = {
  mode: "log_stream_filters";
  run_id: string;
  filters: Array<any>;
};

/** @internal */
export const RecomputeSettingsLogStream$outboundSchema: z.ZodMiniType<
  RecomputeSettingsLogStream$Outbound,
  RecomputeSettingsLogStream
> = z.pipe(
  z.object({
    mode: z.literal("log_stream_filters"),
    runId: z.string(),
    filters: z.array(z.any()),
  }),
  z.transform((v) => {
    return remap$(v, {
      runId: "run_id",
    });
  }),
);

export function recomputeSettingsLogStreamToJSON(
  recomputeSettingsLogStream: RecomputeSettingsLogStream,
): string {
  return JSON.stringify(
    RecomputeSettingsLogStream$outboundSchema.parse(recomputeSettingsLogStream),
  );
}
export function recomputeSettingsLogStreamFromJSON(
  jsonString: string,
): SafeParseResult<RecomputeSettingsLogStream, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RecomputeSettingsLogStream$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RecomputeSettingsLogStream' from JSON`,
  );
}
