/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type DatasetProjectsSort = {
  name: "project_count";
  ascending?: boolean | undefined;
  sortType?: "custom" | undefined;
};

/** @internal */
export type DatasetProjectsSort$Outbound = {
  name: "project_count";
  ascending: boolean;
  sort_type: "custom";
};

/** @internal */
export const DatasetProjectsSort$outboundSchema: z.ZodMiniType<
  DatasetProjectsSort$Outbound,
  DatasetProjectsSort
> = z.pipe(
  z.object({
    name: z.literal("project_count"),
    ascending: z._default(z.boolean(), true),
    sortType: z._default(z.literal("custom"), "custom" as const),
  }),
  z.transform((v) => {
    return remap$(v, {
      sortType: "sort_type",
    });
  }),
);

export function datasetProjectsSortToJSON(
  datasetProjectsSort: DatasetProjectsSort,
): string {
  return JSON.stringify(
    DatasetProjectsSort$outboundSchema.parse(datasetProjectsSort),
  );
}
