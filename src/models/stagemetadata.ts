/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { StageType, StageType$inboundSchema } from "./stagetype.js";

export type StageMetadata = {
  projectId: string;
  stageId: string;
  stageName: string;
  stageVersion: number;
  stageType: StageType;
};

/** @internal */
export const StageMetadata$inboundSchema: z.ZodMiniType<
  StageMetadata,
  unknown
> = z.pipe(
  z.object({
    project_id: types.string(),
    stage_id: types.string(),
    stage_name: types.string(),
    stage_version: types.number(),
    stage_type: StageType$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "project_id": "projectId",
      "stage_id": "stageId",
      "stage_name": "stageName",
      "stage_version": "stageVersion",
      "stage_type": "stageType",
    });
  }),
);

export function stageMetadataFromJSON(
  jsonString: string,
): SafeParseResult<StageMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StageMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StageMetadata' from JSON`,
  );
}
