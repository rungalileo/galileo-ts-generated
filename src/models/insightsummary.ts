/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6ec75d2a9914
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const PriorityCategory = {
  Error: "error",
  Warning: "warning",
  Info: "info",
} as const;
export type PriorityCategory = OpenEnum<typeof PriorityCategory>;

export const Label = {
  Instruction: "Instruction",
  Retrieval: "Retrieval",
  Decision: "Decision",
  Output: "Output",
  Tool: "Tool",
  Reliability: "Reliability",
  Security: "Security",
  Efficiency: "Efficiency",
} as const;
export type Label = OpenEnum<typeof Label>;

export type InsightSummary = {
  /**
   * Signal ID.
   */
  id: string;
  title: string;
  observation: string;
  details: string;
  suggestedAction: string;
  priority: number;
  priorityCategory?: PriorityCategory | null | undefined;
  label?: Label | null | undefined;
};

/** @internal */
export const PriorityCategory$inboundSchema: z.ZodMiniType<
  PriorityCategory,
  unknown
> = openEnums.inboundSchema(PriorityCategory);

/** @internal */
export const Label$inboundSchema: z.ZodMiniType<Label, unknown> = openEnums
  .inboundSchema(Label);

/** @internal */
export const InsightSummary$inboundSchema: z.ZodMiniType<
  InsightSummary,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    title: types.string(),
    observation: types.string(),
    details: types.string(),
    suggested_action: types.string(),
    priority: types.number(),
    priority_category: z.optional(z.nullable(PriorityCategory$inboundSchema)),
    label: z.optional(z.nullable(Label$inboundSchema)),
  }),
  z.transform((v) => {
    return remap$(v, {
      "suggested_action": "suggestedAction",
      "priority_category": "priorityCategory",
    });
  }),
);

export function insightSummaryFromJSON(
  jsonString: string,
): SafeParseResult<InsightSummary, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InsightSummary$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InsightSummary' from JSON`,
  );
}
