/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 18d14a8c6afd
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type TextAggregate = {
  feedbackType: "text";
  count: number;
  unratedCount: number;
};

/** @internal */
export const TextAggregate$inboundSchema: z.ZodMiniType<
  TextAggregate,
  unknown
> = z.pipe(
  z.object({
    feedback_type: types.literal("text"),
    count: types.number(),
    unrated_count: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "feedback_type": "feedbackType",
      "unrated_count": "unratedCount",
    });
  }),
);

export function textAggregateFromJSON(
  jsonString: string,
): SafeParseResult<TextAggregate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TextAggregate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TextAggregate' from JSON`,
  );
}
