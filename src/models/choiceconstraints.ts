/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 93ff4b13614d
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ChoiceConstraints = {
  annotationType: "choice";
  choices: Array<string>;
  allowOther?: boolean | undefined;
};

/** @internal */
export const ChoiceConstraints$inboundSchema: z.ZodMiniType<
  ChoiceConstraints,
  unknown
> = z.pipe(
  z.object({
    annotation_type: types.literal("choice"),
    choices: z.array(types.string()),
    allow_other: z._default(types.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
      "allow_other": "allowOther",
    });
  }),
);
/** @internal */
export type ChoiceConstraints$Outbound = {
  annotation_type: "choice";
  choices: Array<string>;
  allow_other: boolean;
};

/** @internal */
export const ChoiceConstraints$outboundSchema: z.ZodMiniType<
  ChoiceConstraints$Outbound,
  ChoiceConstraints
> = z.pipe(
  z.object({
    annotationType: z.literal("choice"),
    choices: z.array(z.string()),
    allowOther: z._default(z.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      annotationType: "annotation_type",
      allowOther: "allow_other",
    });
  }),
);

export function choiceConstraintsToJSON(
  choiceConstraints: ChoiceConstraints,
): string {
  return JSON.stringify(
    ChoiceConstraints$outboundSchema.parse(choiceConstraints),
  );
}
export function choiceConstraintsFromJSON(
  jsonString: string,
): SafeParseResult<ChoiceConstraints, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ChoiceConstraints$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ChoiceConstraints' from JSON`,
  );
}
