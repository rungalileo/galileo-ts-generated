/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type LogStreamNameSort = {
  name: "name";
  ascending?: boolean | undefined;
  sortType?: "column" | undefined;
};

/** @internal */
export type LogStreamNameSort$Outbound = {
  name: "name";
  ascending: boolean;
  sort_type: "column";
};

/** @internal */
export const LogStreamNameSort$outboundSchema: z.ZodMiniType<
  LogStreamNameSort$Outbound,
  LogStreamNameSort
> = z.pipe(
  z.object({
    name: z.literal("name"),
    ascending: z._default(z.boolean(), true),
    sortType: z._default(z.literal("column"), "column" as const),
  }),
  z.transform((v) => {
    return remap$(v, {
      sortType: "sort_type",
    });
  }),
);

export function logStreamNameSortToJSON(
  logStreamNameSort: LogStreamNameSort,
): string {
  return JSON.stringify(
    LogStreamNameSort$outboundSchema.parse(logStreamNameSort),
  );
}
