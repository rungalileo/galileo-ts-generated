/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: c87cda30c0db
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ScorerVersionHealthScoreEntry = {
  id: string;
  scorerVersionId: string;
  scorerVersionNumber: number;
  datasetId: string;
  healthScoreType: string;
  score: number;
  secondary: { [k: string]: number | null } | null;
  computedAt: Date;
};

/** @internal */
export const ScorerVersionHealthScoreEntry$inboundSchema: z.ZodMiniType<
  ScorerVersionHealthScoreEntry,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    scorer_version_id: types.string(),
    scorer_version_number: types.number(),
    dataset_id: types.string(),
    health_score_type: types.string(),
    score: types.number(),
    secondary: types.nullable(
      z.record(z.string(), types.nullable(types.number())),
    ),
    computed_at: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "scorer_version_id": "scorerVersionId",
      "scorer_version_number": "scorerVersionNumber",
      "dataset_id": "datasetId",
      "health_score_type": "healthScoreType",
      "computed_at": "computedAt",
    });
  }),
);

export function scorerVersionHealthScoreEntryFromJSON(
  jsonString: string,
): SafeParseResult<ScorerVersionHealthScoreEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScorerVersionHealthScoreEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScorerVersionHealthScoreEntry' from JSON`,
  );
}
