/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type LogRecordsSortClause = {
  /**
   * ID of the column to sort.
   */
  columnId: string;
  ascending?: boolean | undefined;
  sortType?: "column" | undefined;
};

/** @internal */
export type LogRecordsSortClause$Outbound = {
  column_id: string;
  ascending: boolean;
  sort_type: "column";
};

/** @internal */
export const LogRecordsSortClause$outboundSchema: z.ZodMiniType<
  LogRecordsSortClause$Outbound,
  LogRecordsSortClause
> = z.pipe(
  z.object({
    columnId: z.string(),
    ascending: z._default(z.boolean(), true),
    sortType: z._default(z.literal("column"), "column" as const),
  }),
  z.transform((v) => {
    return remap$(v, {
      columnId: "column_id",
      sortType: "sort_type",
    });
  }),
);

export function logRecordsSortClauseToJSON(
  logRecordsSortClause: LogRecordsSortClause,
): string {
  return JSON.stringify(
    LogRecordsSortClause$outboundSchema.parse(logRecordsSortClause),
  );
}
