/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 6472c8193532
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type TagsConstraints = {
  annotationType: "tags";
  tags: Array<string>;
  allowOther?: boolean | undefined;
};

/** @internal */
export const TagsConstraints$inboundSchema: z.ZodMiniType<
  TagsConstraints,
  unknown
> = z.pipe(
  z.object({
    annotation_type: types.literal("tags"),
    tags: z.array(types.string()),
    allow_other: z._default(types.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      "annotation_type": "annotationType",
      "allow_other": "allowOther",
    });
  }),
);
/** @internal */
export type TagsConstraints$Outbound = {
  annotation_type: "tags";
  tags: Array<string>;
  allow_other: boolean;
};

/** @internal */
export const TagsConstraints$outboundSchema: z.ZodMiniType<
  TagsConstraints$Outbound,
  TagsConstraints
> = z.pipe(
  z.object({
    annotationType: z.literal("tags"),
    tags: z.array(z.string()),
    allowOther: z._default(z.boolean(), false),
  }),
  z.transform((v) => {
    return remap$(v, {
      annotationType: "annotation_type",
      allowOther: "allow_other",
    });
  }),
);

export function tagsConstraintsToJSON(
  tagsConstraints: TagsConstraints,
): string {
  return JSON.stringify(TagsConstraints$outboundSchema.parse(tagsConstraints));
}
export function tagsConstraintsFromJSON(
  jsonString: string,
): SafeParseResult<TagsConstraints, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TagsConstraints$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TagsConstraints' from JSON`,
  );
}
