/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 04fa0c9edc2e
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  CollaboratorRole,
  CollaboratorRole$inboundSchema,
} from "./collaboratorrole.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import { Permission, Permission$inboundSchema } from "./permission.js";

/**
 * User collaborator for an annotation queue, extends shared UserCollaborator with annotation_queue_id.
 */
export type UserAnnotationQueueCollaborator = {
  id: string;
  permissions?: Array<Permission> | undefined;
  role: CollaboratorRole;
  createdAt: Date;
  userId: string;
  firstName: string | null;
  lastName: string | null;
  email: string;
  annotationQueueId: string;
  trackProgress: boolean;
  progress?: number | null | undefined;
};

/** @internal */
export const UserAnnotationQueueCollaborator$inboundSchema: z.ZodMiniType<
  UserAnnotationQueueCollaborator,
  unknown
> = z.pipe(
  z.object({
    id: types.string(),
    permissions: types.optional(z.array(Permission$inboundSchema)),
    role: CollaboratorRole$inboundSchema,
    created_at: types.date(),
    user_id: types.string(),
    first_name: types.nullable(types.string()),
    last_name: types.nullable(types.string()),
    email: types.string(),
    annotation_queue_id: types.string(),
    track_progress: z._default(types.boolean(), true),
    progress: z.optional(z.nullable(types.number())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "user_id": "userId",
      "first_name": "firstName",
      "last_name": "lastName",
      "annotation_queue_id": "annotationQueueId",
      "track_progress": "trackProgress",
    });
  }),
);

export function userAnnotationQueueCollaboratorFromJSON(
  jsonString: string,
): SafeParseResult<UserAnnotationQueueCollaborator, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UserAnnotationQueueCollaborator$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UserAnnotationQueueCollaborator' from JSON`,
  );
}
