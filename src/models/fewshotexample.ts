/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Few-shot example for a chainpoll metric prompt.
 */
export type FewShotExample = {
  generationPromptAndResponse: string;
  evaluatingResponse: string;
};

/** @internal */
export const FewShotExample$inboundSchema: z.ZodMiniType<
  FewShotExample,
  unknown
> = z.pipe(
  z.object({
    generation_prompt_and_response: types.string(),
    evaluating_response: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "generation_prompt_and_response": "generationPromptAndResponse",
      "evaluating_response": "evaluatingResponse",
    });
  }),
);
/** @internal */
export type FewShotExample$Outbound = {
  generation_prompt_and_response: string;
  evaluating_response: string;
};

/** @internal */
export const FewShotExample$outboundSchema: z.ZodMiniType<
  FewShotExample$Outbound,
  FewShotExample
> = z.pipe(
  z.object({
    generationPromptAndResponse: z.string(),
    evaluatingResponse: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      generationPromptAndResponse: "generation_prompt_and_response",
      evaluatingResponse: "evaluating_response",
    });
  }),
);

export function fewShotExampleToJSON(fewShotExample: FewShotExample): string {
  return JSON.stringify(FewShotExample$outboundSchema.parse(fewShotExample));
}
export function fewShotExampleFromJSON(
  jsonString: string,
): SafeParseResult<FewShotExample, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FewShotExample$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FewShotExample' from JSON`,
  );
}
