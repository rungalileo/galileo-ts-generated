/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

/**
 * This global operation filters a set of rows and discard the rest.
 */
export type DatasetFilterRows = {
  editType: "filter_rows";
  rowIds: Array<string>;
};

/** @internal */
export type DatasetFilterRows$Outbound = {
  edit_type: "filter_rows";
  row_ids: Array<string>;
};

/** @internal */
export const DatasetFilterRows$outboundSchema: z.ZodMiniType<
  DatasetFilterRows$Outbound,
  DatasetFilterRows
> = z.pipe(
  z.object({
    editType: z.literal("filter_rows"),
    rowIds: z.array(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      editType: "edit_type",
      rowIds: "row_ids",
    });
  }),
);

export function datasetFilterRowsToJSON(
  datasetFilterRows: DatasetFilterRows,
): string {
  return JSON.stringify(
    DatasetFilterRows$outboundSchema.parse(datasetFilterRows),
  );
}
