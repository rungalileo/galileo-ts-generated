/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Configuration for a custom LiteLLM handler class.
 *
 * @remarks
 *
 * Allows users to specify a custom implementation of litellm.CustomLLM
 * that handles acompletion() calls with custom request/response transformation.
 */
export type CustomLLMConfig = {
  /**
   * Python file name containing the CustomLLM class (e.g., 'my_handler.py')
   */
  fileName: string;
  /**
   * Class name within the module (must be a litellm.CustomLLM subclass)
   */
  className: string;
  /**
   * Optional keyword arguments to pass to the CustomLLM constructor
   */
  initKwargs?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const CustomLLMConfig$inboundSchema: z.ZodMiniType<
  CustomLLMConfig,
  unknown
> = z.pipe(
  z.object({
    file_name: types.string(),
    class_name: types.string(),
    init_kwargs: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      "file_name": "fileName",
      "class_name": "className",
      "init_kwargs": "initKwargs",
    });
  }),
);
/** @internal */
export type CustomLLMConfig$Outbound = {
  file_name: string;
  class_name: string;
  init_kwargs?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const CustomLLMConfig$outboundSchema: z.ZodMiniType<
  CustomLLMConfig$Outbound,
  CustomLLMConfig
> = z.pipe(
  z.object({
    fileName: z.string(),
    className: z.string(),
    initKwargs: z.optional(z.nullable(z.record(z.string(), z.any()))),
  }),
  z.transform((v) => {
    return remap$(v, {
      fileName: "file_name",
      className: "class_name",
      initKwargs: "init_kwargs",
    });
  }),
);

export function customLLMConfigToJSON(
  customLLMConfig: CustomLLMConfig,
): string {
  return JSON.stringify(CustomLLMConfig$outboundSchema.parse(customLLMConfig));
}
export function customLLMConfigFromJSON(
  jsonString: string,
): SafeParseResult<CustomLLMConfig, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CustomLLMConfig$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CustomLLMConfig' from JSON`,
  );
}
