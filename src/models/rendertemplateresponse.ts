/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  RenderedTemplate,
  RenderedTemplate$inboundSchema,
} from "./renderedtemplate.js";

export type RenderTemplateResponse = {
  startingToken: number;
  limit: number;
  paginated: boolean;
  nextStartingToken?: number | null | undefined;
  renderedTemplates: Array<RenderedTemplate>;
};

/** @internal */
export const RenderTemplateResponse$inboundSchema: z.ZodMiniType<
  RenderTemplateResponse,
  unknown
> = z.pipe(
  z.object({
    starting_token: z._default(types.number(), 0),
    limit: z._default(types.number(), 100),
    paginated: z._default(types.boolean(), false),
    next_starting_token: z.optional(z.nullable(types.number())),
    rendered_templates: z.array(RenderedTemplate$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "starting_token": "startingToken",
      "next_starting_token": "nextStartingToken",
      "rendered_templates": "renderedTemplates",
    });
  }),
);

export function renderTemplateResponseFromJSON(
  jsonString: string,
): SafeParseResult<RenderTemplateResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RenderTemplateResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RenderTemplateResponse' from JSON`,
  );
}
