/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 5ac353718006
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  HealthScoreType,
  HealthScoreType$inboundSchema,
} from "./healthscoretype.js";

export type HealthScoreResult = {
  healthScoreType: HealthScoreType | null;
  /**
   * Primary health score metric value, or None if no valid rows.
   */
  value: number | null;
  /**
   * Rows excluded because MGT or score could not be parsed.
   */
  skippedRows: number;
  /**
   * Secondary metrics (MAE, RMSE, R², per-class F1, etc.).
   */
  secondary: { [k: string]: number | null };
  /**
   * Rows with a successful scorer result.
   */
  totalScoredRows: number;
  /**
   * Rows with a non-null MGT value after overlay.
   */
  totalMgtRows: number;
  /**
   * Rows with both a score and a MGT value (used for computation).
   */
  joinedRows: number;
};

/** @internal */
export const HealthScoreResult$inboundSchema: z.ZodMiniType<
  HealthScoreResult,
  unknown
> = z.pipe(
  z.object({
    health_score_type: types.nullable(HealthScoreType$inboundSchema),
    value: types.nullable(types.number()),
    skipped_rows: types.number(),
    secondary: z.record(z.string(), types.nullable(types.number())),
    total_scored_rows: types.number(),
    total_mgt_rows: types.number(),
    joined_rows: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "health_score_type": "healthScoreType",
      "skipped_rows": "skippedRows",
      "total_scored_rows": "totalScoredRows",
      "total_mgt_rows": "totalMgtRows",
      "joined_rows": "joinedRows",
    });
  }),
);

export function healthScoreResultFromJSON(
  jsonString: string,
): SafeParseResult<HealthScoreResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => HealthScoreResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'HealthScoreResult' from JSON`,
  );
}
