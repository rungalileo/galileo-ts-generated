/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type ScoreRating = {
  feedbackType: "score";
  value: number;
};

/** @internal */
export const ScoreRating$inboundSchema: z.ZodMiniType<ScoreRating, unknown> = z
  .pipe(
    z.object({
      feedback_type: types.literal("score"),
      value: types.number(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "feedback_type": "feedbackType",
      });
    }),
  );

export function scoreRatingFromJSON(
  jsonString: string,
): SafeParseResult<ScoreRating, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScoreRating$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScoreRating' from JSON`,
  );
}
