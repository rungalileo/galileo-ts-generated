/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Reference to a file associated with this message.
 *
 * @remarks
 *
 * The file_id can be resolved via the ``files`` dict returned on
 * trace/span detail responses, which contains metadata such as
 * modality, MIME type, and a presigned download URL.
 */
export type FileContentPart = {
  type: "file";
  fileId: string;
};

/** @internal */
export const FileContentPart$inboundSchema: z.ZodMiniType<
  FileContentPart,
  unknown
> = z.pipe(
  z.object({
    type: types.literal("file"),
    file_id: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "file_id": "fileId",
    });
  }),
);
/** @internal */
export type FileContentPart$Outbound = {
  type: "file";
  file_id: string;
};

/** @internal */
export const FileContentPart$outboundSchema: z.ZodMiniType<
  FileContentPart$Outbound,
  FileContentPart
> = z.pipe(
  z.object({
    type: z.literal("file"),
    fileId: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      fileId: "file_id",
    });
  }),
);

export function fileContentPartToJSON(
  fileContentPart: FileContentPart,
): string {
  return JSON.stringify(FileContentPart$outboundSchema.parse(fileContentPart));
}
export function fileContentPartFromJSON(
  jsonString: string,
): SafeParseResult<FileContentPart, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FileContentPart$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FileContentPart' from JSON`,
  );
}
