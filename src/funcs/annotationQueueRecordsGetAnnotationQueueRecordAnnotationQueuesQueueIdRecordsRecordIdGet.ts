/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 2111a4d7266c
 */

import * as z from "zod/v4-mini";
import { GalileoGeneratedCore } from "../core.js";
import { encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { resolveOAuth2Password, resolveSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { GalileoGeneratedError } from "../models/errors/galileogeneratederror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Get Annotation Queue Record
 *
 * @remarks
 * Get a single record in an annotation queue.
 *
 * Permission checks:
 * - User must have READ permission on the annotation queue
 */
export function annotationQueueRecordsGetAnnotationQueueRecordAnnotationQueuesQueueIdRecordsRecordIdGet(
  client: GalileoGeneratedCore,
  security:
    operations.GetAnnotationQueueRecordAnnotationQueuesQueueIdRecordsRecordIdGetSecurity,
  request:
    operations.GetAnnotationQueueRecordAnnotationQueuesQueueIdRecordsRecordIdGetRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    operations.ResponseGetAnnotationQueueRecordAnnotationQueuesQueueIdRecordsRecordIdGet,
    | errors.HTTPValidationError
    | GalileoGeneratedError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    security,
    request,
    options,
  ));
}

async function $do(
  client: GalileoGeneratedCore,
  security:
    operations.GetAnnotationQueueRecordAnnotationQueuesQueueIdRecordsRecordIdGetSecurity,
  request:
    operations.GetAnnotationQueueRecordAnnotationQueuesQueueIdRecordsRecordIdGetRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      operations.ResponseGetAnnotationQueueRecordAnnotationQueuesQueueIdRecordsRecordIdGet,
      | errors.HTTPValidationError
      | GalileoGeneratedError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) =>
      z.parse(
        operations
          .GetAnnotationQueueRecordAnnotationQueuesQueueIdRecordsRecordIdGetRequest$outboundSchema,
        value,
      ),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const pathParams = {
    queue_id: encodeSimple("queue_id", payload.queue_id, {
      explode: false,
      charEncoding: "percent",
    }),
    record_id: encodeSimple("record_id", payload.record_id, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path = pathToFunc("/annotation_queues/{queue_id}/records/{record_id}")(
    pathParams,
  );

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const requestSecurity = resolveSecurity(
    [
      {
        fieldName: "Splunk-AO-API-Key",
        type: "apiKey:header",
        value: security?.apiKeyHeader,
      },
    ],
    [
      {
        fieldName: "Authorization",
        type: "oauth2:password",
        value: resolveOAuth2Password(security?.oAuth2PasswordBearer, {
          defaults: { tokenURL: "https://api.galileo.ai/login" },
        }),
      },
    ],
    [
      {
        type: "http:basic",
        value: {
          username: security?.httpBasic?.username,
          password: security?.httpBasic?.password,
        },
      },
    ],
  );

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID:
      "get_annotation_queue_record_annotation_queues__queue_id__records__record_id__get",
    oAuth2Scopes: [],

    resolvedSecurity: requestSecurity,

    securitySource: security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    operations.ResponseGetAnnotationQueueRecordAnnotationQueuesQueueIdRecordsRecordIdGet,
    | errors.HTTPValidationError
    | GalileoGeneratedError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(
      200,
      operations
        .ResponseGetAnnotationQueueRecordAnnotationQueuesQueueIdRecordsRecordIdGet$inboundSchema,
    ),
    M.jsonErr(422, errors.HTTPValidationError$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
